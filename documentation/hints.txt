// codepad

BUG pthread_create: Resource temporarily unavailable

https://unix.stackexchange.com/questions/253903/creating-threads-fails-with-resource-temporarily-unavailable-with-4-3-kernel

BUG network unreachable

restart systemd-networkd on the host, .. and on the containers

BUG container restarts fail

remove --kep-unit

https://github.com/systemd/systemd/issues/2809


# shellcheck disable=SC2086

## Get base64 encoded login code for user x pass xxxxxx
## echo -en "\0x\0xxxxxx" | base64
## AHgAeHh4eHh4

#### IMAP4S connection test
## openssl s_client -crlf -connect test.d250.hu:993
## a LOGIN user passwd
## QUIT

#### SASL commands
## saslauthd -a rimap -O localhost
## saslauthd -d -a rimap -O localhost
## testsaslauthd -u username -p password
## testsaslauthd -u x -p xxxxxx
## testsaslauthd -u x@test.d250.hu -p xxxxxx
##

#### SMTPS connection test 
## openssl s_client -connect test.d250.hu:465
## EHLO d250.hu
## AUTH LOGIN

“VXNlcm5hbWU6” is the BASE64 encoded text for the word "Username" and “UGFzc3dvcmQ6” is the BASE64 encoded text for the word "Password"
respond with base64 encoded strings

Saslauthd has problems. It can lag, ...
we try now using -n 0
-n threads
Use threads processes for responding to authentication queries. (default: 5) A value of zero will indicate that saslauthd should fork an individual process for each connection. This can solve leaks that occur in some deployments..

## ... clear logs
journalctl --rotate
journalctl --vacuum-time=1s

## ... still a bug - dovecot key size
https://bugzilla.redhat.com/show_bug.cgi?id=1882939

## .. firewalld 
all bridges must be on the trusted zone


x@test.d250.hu Ehafu-Enugo

# echo -en "\0x\0Ehafu-Enugo" | base64
AHgARWhhZnUtRW51Z28=

openssl s_client -crlf -connect localhost:993
a LOGIN x@test.d250.hu Ehafu-Enugo

#### plaintext imap login test
telnet test.d250.hu 143
a LOGIN x Ehafu-Enugo

## To disable imap, auth must allow plaintext, and ssl must be no

## postfix patch

dnf -y install cyrus-sasl cyrus-sasl-lib cyrus-sasl-plain cyrus-sasl-md5

smtpd_tls_cert_file = /etc/pki/tls/certs/postfix.pem
smtpd_tls_key_file = /etc/pki/tls/private/postfix.key
smtpd_use_tls = yes
smtpd_tls_session_cache_database = btree:${data_directory}/smtpd_scache
smtp_tls_session_cache_database = btree:${data_directory}/smtp_scache

smtpd_sasl_auth_enable = yes
smtpd_sasl_security_options = noanonymous
smtpd_sasl_local_domain = $myhostname
broken_sasl_auth_clients = yes
smtpd_recipient_restrictions = permit_sasl_authenticated, permit_mynetworks, reject_unauth_destination


