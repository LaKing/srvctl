#!/bin/bash
## modules/gui/make_node_modules.sh - manual helper for the dormant gui
## module: creates a local node_modules for server.js instead of the npm
## globals the (disabled) install hook would set up. Not called by srvctl.

## the packages get installed globally, but in case you need a local node_modules folder ...

## FIXME(v4): low - uses the srvctl helper `run` but is never sourced under
## srvctl and does not source lablib.sh, so standalone every line fails with
## "run: command not found" and nothing gets installed. Note also that
## package.json still lists the dead pty.js instead of the node-pty
## dependency installed here and required by server.js.
rm -fr node_modules

run dnf -y install gcc-c++

run npm install node-pty
#run npm install pty.js
run npm install express
run npm install socket.io
run npm install ssh2
