## harness-a fixture module — always enabled (deterministic). The sandbox
## harness (modules/srvctl/selftest/sandbox/run-harness.sh) enables it to
## exercise the real bash help path against known command metadata.
echo true
