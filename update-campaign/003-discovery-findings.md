# 003 — Discovery findings (session 1, commit 988c38c)

Consolidated from the 38 discovery fact sheets (see modules/*.md and
core.md for full context). 301 findings: 1 blocker, 34 high, 115 medium, 151 low.

Per 000-PROMPT FILING these are consolidated here rather than one file per
issue; individually-numbered issues get created when a finding becomes a
work package. NONE of these are fixed by the overnight polish pass unless
they meet the 002 "unambiguous mechanical defect" bar (polish agents list
what they fixed; everything else gets a FIXME(v4) marker).


## BLOCKER (1)

- **srvctl** `modules/srvctl/commands/update-install.sh:91` — `cat completion.sh > /etc/bash_completion.d/srvctl-completion` writes through the symlink init.sh:36-44 creates back to the same source file on standard installs, so the redirection truncates modules/srvctl/completion.sh to 0 bytes (verified `cat f > f` empties f), destroying completion and dirtying the install-dir git tree the v4 rollout rides on.

## HIGH (34)

- **backup** `modules/backup/libs/7zlib.sh:28` — Bare `exit` (added in e179f53) makes local_container_7z_backup terminate the entire srvctl process with status 0 before doing anything, so any automated caller silently believes the container backup succeeded while nothing was backed up and the rest of the calling script never runs (lines 30-91 are dead code).
- **backupdb** `modules/backupdb/libs/backupdblib.sh:27` — rm -fr /root/mongodb-dump/* deletes the only existing MongoDB dump before the new dump is attempted, so any mongodump failure leaves the machine with zero backups.
- **codepad** `modules/codepad/access.js:108-113` — Copies each authorized user's plaintext .password file (also their host login password per usersonhost/main.js:91-96) into /var/srvctl3/share/containers/<c>/users/<u>/ which is bind-mounted into container c, so any process/user inside a shared devel container can read every other authorized user's, admin's, and reseller's plaintext password.
- **containers** `modules/containers/commands/recreate-ve.sh:43-48` — tmp_rootfs is never removed and the rootfs mv is skipped when it exists, so a repeated recreate-ve rsyncs the stale tmp_rootfs snapshot (wp-content, mysql, home, srv) over the live rootfs, silently overwriting current data.
- **containers** `modules/containers/libs/statuslib.sh:27` — all_containers runs 'machinectl $cop' for non-start ops but machinectl has no stop/restart verb, so 'sc all-containers stop' stops nothing and 'sc all-containers restart' stops every container via systemctl and never starts them again.
- **containers** `modules/containers/libs/backupcontainerlib.sh:30` — run "ssh $SC_BACKUP_HOST 'mkdir -p $path'" passes the single-quoted string as one remote word (command not found), so the remote directory is never created and the subsequent rsync+exif aborts backup-ve — remote backups are broken whenever SC_BACKUP_HOST is set.
- **core** `commonlib.sh:383` — help_commands tests -f $dir/commands/$arg without the .sh suffix, so 'sc help <module-command>' never matches any module command and always prints "Pardon? '<cmd>' is not a command" (and exits 0 via init.sh:169-170) — per-command help is broken for every module command.
- **core** `commonlib.sh:131` — Operator precedence in the datastore-verb dispatch makes the root+has-args guard bind only to 'new', so any user (non-root, or root with no args) running sc get/put/out/cfg/del/add reaches the datastore shell functions directly, bypassing the intended root-only gate on mutation verbs.
- **dns** `modules/dns/hooks/update-install-host.sh:5` — Overwrites /etc/systemd/resolved.conf with a bare `DNS=8.8.8.8` line lacking the [Resolve] section header, so systemd-resolved ignores the assignment ("Assignment outside of section") and the advertised global DNS change never takes effect while the stock config file is destroyed on every `sc update-install`.
- **gui** `modules/gui/hooks/update-install-host.sh:3` — Entire hook body is inside `if false; then ... fi`, so the GUI service, certificates, npm dependencies and firewall rule are never installed — the module is undeployable dead weight while SC_USE_GUI=true still loads spec.sh and runs make_commands_spec on every host.
- **haproxy** `modules/haproxy/commands/http-redirect.sh:29` — [[ $SC_UID0 ]] tests non-emptiness but SC_UID0 is always the string 'true' or 'false' (init.sh:129-131), so the authorization gate always passes, the 'no access' branch at line 35 is unreachable, and non-owner users proceed into the privileged put/regenerate path (blocked today only by datastore file permissions, yielding a misleading DATASTORE-ERROR).
- **letsencrypt** `modules/letsencrypt/letsencrypt-ca.pem:1` — Vendored CA is DST Root CA X3 with notAfter Sep 30 2021 (expired); it is concatenated into every deployed bundle and container localhost.crt (used at libs/letsencryptlib.sh:43 and letsencrypt.js:173,181,195), shipping an expired root in the presented TLS chain and risking validation failures on strict clients.
- **mariadb** `modules/mariadb/libs/mariadblib.sh:43` — check_mariadb_connection uses bare `exit` after err (status 0), so a failed DB connection terminates srvctl with exit code 0 and automated backup callers see success while no backup was made.
- **named** `modules/named/commands/override-in-address.sh:26` — [[ $SC_UID0 ]] tests string non-emptiness but SC_UID0 is always the literal 'true' or 'false' (init.sh:129-131), so the authorization check is always true and the access-denied branch (lines 31-33) is unreachable — non-owner users proceed to the datastore put, stopped only by file permissions.
- **named** `modules/named/installnamedlib.sh:70` — Generated /etc/named.conf hard-codes include "/var/named/d250.conf", a company-specific file nothing in the repo creates; on any fresh deployment without it, named fails to load its config and DNS is completely down.
- **named** `modules/named/apps/dyndns-server.js:80` — Dormant dyndns server passes the raw request URL (dyndnshost) unsanitized into exec("/bin/bash ... " + dyndnshost) — shell command injection as root/uid103, plus path traversal via fs.readFile("/var/dyndns/"+dyndnshost+".auth") at line 64; currently unreachable because install_dyndns is disabled, but must not be revived as-is.
- **odoo** `modules/odoo/commands/install-odoo.sh:162` — dnf install list contains package name 'node' which nothing in Fedora provides (verified via dnf repoquery --whatprovides), aborting the whole transaction so no dependencies get installed, with all output discarded to /dev/null.
- **odoo** `modules/odoo/commands/install-odoo.sh:145` — The installer's only error handling, set -e, is inert because the file is sourced inside the `if run_command` condition (srvctl.sh:111) where bash ignores errexit, and the ERR trap is only installed at line 371 after all work — so every failure (dnf, initdb, git, pip) is silently skipped and line 373 still reports success.
- **odoo** `modules/odoo/commands/install-odoo.sh:152-154` — Hardcoded hr_HR.UTF-8 locale is exported for initdb and written into template0/template1 (lines 196, 203-204) but glibc-langpack-hr is never installed, so on minimal Fedora container images postgresql-setup --initdb fails, the odoo role is never created, and odoo.service crash-loops.
- **openvpn** `modules/openvpn/hooks/update-install-host.sh:86` — Unit-name typo `openvpn-cleint@hostnet-client-$host.service` in the restart call means client tunnels on Fedora 28+ are enabled but never (re)started during update-install, so new or changed client configs take effect only after a reboot or manual restart.
- **openvpn** `modules/openvpn/libs/openvpnconfiglib.sh:13` — The fedora-28 block creates /etc/openvpn/server/hostnet-ccd but writes DEFAULT to the fedora-27 path /etc/openvpn/hostnet-ccd/DEFAULT again, so openvpn-server@ (WorkingDirectory=/etc/openvpn/server) reads an empty client-config-dir and the iroute directive is never applied on modern hosts.
- **password** `modules/password/lib.js:5 and modules/password/libs/get-password.sh:19` — Security-critical credentials (MariaDB, WordPress admin, Linux user passwords, SSL passphrases) are generated with non-cryptographic RNGs (V8 Math.random xorshift128+ and bash $RANDOM) over a ~39-bit pattern space, making them feasible offline-cracking targets.
- **perdition** `modules/perdition/hooks/update-install-host.sh:3` — The only call to install_perdition is commented out (commit e179f53), so update-install never installs perdition/its units//var/perdition on a fresh host, and the still-active regenerate hook then fails hard (perdition.js exits 111 on ENOENT writing /var/perdition/popmap.re, exif in libs/bashlib.sh:9 aborts), killing every add-ve/regenerate on that host.
- **ssh** `modules/ssh/ssh.js:106` — copy_user_key matches key files with split(".")[1]==="pub", so keys named <user>-$NOW.pub by add-publickey (NOW=%Y.%m.%d-%H:%M:%S contains dots) are silently never copied to containers, breaking container ssh access for user-added keys.
- **ssh** `modules/ssh/ssh.js:79-114` — Key distribution is add-only and regenerate_ssh_config (libs/sshlib.sh:6) deletes only files named authorized_keys, so revoked/removed users' copied .pub files under /var/srvctl3/share/containers/<C>/users/ keep granting root login to containers indefinitely.
- **sshpiperd** `modules/sshpiperd/sshpiperd (deployed via hooks/update-install-host.sh:26)` — Vendored binary is stale relative to its source: its strings contain srvctl_id_rsa (banner: srvctl 3.1.2.1) while workingdir.go:30 and the ssh/usersonhost modules only create srvctl_id_ecdsa, so the deployed daemon looks up a per-user private key file srvctl never generates and upstream key mapping fails on fresh installs.
- **sshpiperd** `modules/sshpiperd/workingdir.go:137-138` — strings.Split(user,"_")[1]/[2] executes before the checkUsername regexp check (line 140), so any SSH login with fewer than two underscores in the username (e.g. bots trying 'root' on the world-open port 2222) causes an index-out-of-range panic — an unauthenticated remote DoS (systemd restarts the daemon every 3s).
- **static** `modules/static/server.js:27-35` — finalhandler onerror callback calls res.send(), which does not exist on http.ServerResponse (Express API), so the deferred TypeError is an uncaught exception that crashes the whole server on any request producing a serve-static error (e.g. GET /% → 400), giving a trivial remote DoS despite Restart=always.
- **static** `modules/static/server.js:37` — Document root is built from the unvalidated Host header ('/var/srvctl3/storage/static/' + host + '/html'); '/' and '.' are legal header bytes, so Host: ../../../srv/<ve>/rootfs/var/www escapes the storage tree and the root-run process serves any readable directory ending in /html, e.g. other tenants' container webroots.
- **usersonhost** `modules/usersonhost/commands/add-publickey.sh:51` — `run ssh-keygen -i -f "$pub.tmp" > "$pub"` redirects run()'s whole stdout — including its colored command banner — into $pub, corrupting the converted key so the follow-up ssh-keygen -l fails and the key is deleted; the RFC4716→OpenSSH conversion path is broken.
- **usersonhost** `modules/usersonhost/main.js:90` — `msg("... password: " + password)` prints the user's plaintext password to stdout and srvctl logs on every password change (the bash equivalent is commented out), a sensitive-data disclosure.
- **vncproxy** `modules/vncproxy/commands/add-vnc-user.sh:14` — Username regex is unanchored so any string containing one lowercase letter passes; the unvalidated name flows into /var/vncproxy/records which start.sh:34 sources as root bash and start.sh:24 splices into a raw sqlite INSERT, giving root command execution / SQL injection at service start.
- **wordpress** `modules/wordpress/scripts/restore-wordpress-password.sh:61` — The sanity check `mysql "$SC_MDA" -e "use $1; 'show tables';"` is a guaranteed SQL syntax error (bare string literal statement), so the script always exits before the password UPDATE at :79 — the admin-password recovery tool is completely non-functional.
- **wordpress** `modules/wordpress/scripts/restore-wordpress-password.sh:32` — Fallback SC_MDA="-u root -p" is passed quoted as one argument in every mysql call (:35,47,61,69,79,87), so without /etc/mysqldump.conf every invocation fails at the connection check, making restore impossible on such containers.

## MEDIUM (115)

- **backup** `modules/backup/libs/7zlib.sh:11` — Uses BACKUP_PATH while sclib.sh:3 and containers/libs/backupcontainerlib.sh:8 use SC_BACKUP_PATH, so an admin setting SC_BACKUP_PATH still gets 7z container archives under the /backup default — divergent backup locations from one intended setting.
- **backup** `modules/backup/libs/sclib.sh:48` — Concurrency guard `systemctl status | grep rsync | grep $target | grep -c $i` (also lines 142, 240) is a substring match over the whole process tree, so backing up /srv while any rsync mentioning /srv2 (or any process cmdline containing both strings) is running silently skips that directory's backup via continue.
- **backup** `modules/backup/libs/sclib.sh:90` — Outside a TTY (cron) the geometry block is skipped so size/count variables are empty, and every non-interactive OK/error log line (also 188, 288 and error lines 92, 190, 290) is written malformed as `#size: / files: /`, degrading the only success record the module produces.
- **backupdb** `modules/backupdb/libs/backupdblib.sh:30` — mongodump exit status is never checked (no exif/eyif), so a failed or partial dump leaves an empty timestamp directory that automation cannot distinguish from success, unlike the mariadb branch which hard-exits on error.
- **backupdb** `modules/backupdb/libs/backupdblib.sh:30` — Unconditionally uses vendored x86_64-only mongo-tools r3.6.0 (2017) even when a modern /usr/bin/mongodump exists, so dumps fail against MongoDB 4.2+ servers and on non-x86_64 hosts — after the old dump was already deleted.
- **backupdb** `modules/backupdb/libs/backupdblib.sh:13` — MongoDB detection keys only on /var/lib/mongodb (Fedora datadir); installs from mongodb.org RPMs use /var/lib/mongo, so those databases are silently never backed up.
- **backupdb** `modules/backup/libs/7zlib.sh:38` — The only automated invocation of this unit runs 'srvctl backup-db clean', a command that exists in no module (the unit only defines the function backupdb), so it prints 'Invalid command. backup-db' and DB dumps never happen during container backups.
- **ca** `modules/ca/libs/calib.sh:161` — When an expired/invalid usernet client cert+key are deleted and re-issued (calib.sh:108-119), the .p12 bundle is only created if missing, so the stale .p12 containing the old expired/mismatched key is never regenerated and users import a dead identity.
- **ca** `modules/ca/libs/netlib.sh:12` — ca_sync mirrors the entire CA directory (root CA private keys, all host/user private keys, passwordless .p12s) onto every non-CA host on every regenerate, so compromising any single cluster host compromises the whole PKI.
- **ca** `modules/ca/libs/calib.sh:97` — Server-cert signing uses modules/certificates/openssl-server-ext.cnf instead of the module's own identical (dead) copy, so removing/renaming that other module's file silently breaks all server certificate issuance (run does not abort on openssl failure).
- **certificates** `modules/certificates/libs/wildcardcertlib.sh:59` — Condition `[[ $c == "$SC_COMPANY_DOMAIN" ]] && [[ ${c} != *"."* ]]` is self-contradictory for any dotted company domain (first operand should be $checked_domain), so dot-less container names silently never receive the company wildcard certificate.
- **certificates** `modules/certificates/libs/servicecertlib.sh:115` — Fatal could-not-locate-certificate path runs bare `exit` after err (which returns 0), terminating srvctl with exit code 0 and masking the failure from update-install wrappers and cron.
- **certificates** `modules/certificates/libs/domaincertlib.sh:98` — `run openssl verify -CAfile "$ssl_pem $ssl_pem"` only works via run's unquoted $* word-splitting, and the `$? == 2` branch labeled 'already has a Self signed certificate' actually fires on verification FAILURE (verified on OpenSSL 3.5: self-signed verifies exit 0, CA-signed leaf against itself exits 2), so CA-signed certs take an early return without refreshing cert.pem or checking the key file, and every such check logs a spurious ERROR to SC_LOG via run→eyif.
- **certificates** `modules/certificates/libs/domaincertlib.sh:197` — Combined pem files containing the private key (also cert.pem at :204 and wildcard copies at wildcardcertlib.sh:55,61) are created by shell redirection with default umask (0644) and never chmod'ed, leaving key material world-readable except where parent-dir modes from the unrelated set_permissions happen to protect them.
- **certificates** `modules/certificates/libs/servicecertlib.sh:100` — Generates 1024-bit DH parameters (`openssl dhparam 1024`) appended to every service crt.pem — Logjam-weak and rejected by modern TLS stacks.
- **codepad** `modules/codepad/libs/mkrootfs_fedora_install_codepad.sh:31` — `run chroot "$rootfs" chown codepad:codepad "$rootfs"/srv/codepad-project` doubles the path inside the chroot so the chown always fails (only an eyif warning), leaving /srv/codepad-project root-owned in the template and unwritable by the codepad service.
- **codepad** `modules/codepad/libs/mkrootfs_fedora_install_codepad.sh:23` — Bare `exit` after `err` exits with status 0 (err returns 0), so a missing/failed codepad rootfs terminates `sc regenerate rootfs` reporting success, masking the build failure.
- **codepad** `modules/codepad/libs/mkrootfs_fedora_install_codepad.sh:47` — git clone uses the host template path as source, baking /var/srvctl3/rootfs/codepad/var/git as the origin URL into every container's /srv/codepad-project/.git/config; inside containers that path does not exist so push/pull to origin always fails.
- **containers** `modules/containers/libs/regenlib.sh:133` — [[ $(get container C user_ip_match) ]] is truthy for both "true" and "false", so the IP-mismatch repair branch of check_container_ownership is unreachable and misassigned container IPs are never fixed.
- **containers** `modules/containers/commands/destroy-ve.sh:31` — [[ $SC_UID0 ]] is always true because SC_UID0 holds the string true/false (same defect in remove-ve.sh:31 and backupcontainerlib.sh:10), making the 'no access' authorization branch dead code and letting non-sudomized users into the root-only path.
- **containers** `modules/containers/libs/authlib.sh:4` — hs_only/ve_only test never-assigned $SC_ON_HS/$SC_ON_VE, so an unset variable makes the guard always pass; host-only/VE-only enforcement exists only via the help-listing grep and module conditions, and ve_only-marked commands run unchecked on hosts.
- **containers** `modules/containers/libs/regenlib.sh:97-100` — check_container_directories calls create_nspawn_container_filesystem for any /srv entry lacking rootfs; with no datastore type the empty-T path passes the '-d $SC_ROOTFS_DIR/' check and copies ALL base images into /srv/<straydir>/rootfs (and creates a literal /srv/* dir when /srv is empty), after which the stray dir is auto-imported as a container.
- **containers** `modules/containers/hooks/adjust-service.sh:30` — Ownership check '[[ user match ]] || ... || $SC_UID0' inside 'if $SC_UID0' is a tautology, so the AUTH-ERROR exit 142 branch is dead and any user who can sudomize srvctl can start/stop/restart any other user's container.
- **containers** `modules/containers/status.js:151` — users[containers[c].user].reseller (also .type.tab at :163 and users[SC_USER].access at :96) throws an uncaught TypeError when a referenced user record or type key is missing, aborting the whole 'sc status' listing with exit 99.
- **containers** `modules/containers/hooks/update-install-host.sh:30` — 'chown 750 /var/srvctl3/share/containers' passes a mode as owner, leaving the directory owned by non-existent UID 750 instead of chmod 750.
- **core** `lablib.sh:110` — Inside run(), eyif reads $? of the just-succeeded if-condition (always 0) instead of the saved $__exitcode, so the "command ... returned with an error" warning can never print (verified empirically); the systemctl/status exemption at line 108 is also logically inverted — all run() failures are silent unless the caller adds exif/eyif.
- **core** `commonlib.sh:234` — head "$file" | grep -m1 "$HEXE" "$file" passes the file as an operand so grep ignores the head-limited stdin and scans the whole file (same for HEMP at :231); any '## &&&' line anywhere in a command file (e.g. inside a heredoc) is executed via command substitution at :237 on every hint_commands run — unintended command execution.
- **core** `commonlib.sh:448` — Module cache regeneration appends (>>) to $SC_HOME/.srvctl/modules.conf without truncating and update-install only removes /var/local/srvctl/modules.conf (init.sh:84), so the per-user/root cache grows unboundedly across update-installs and stale SC_USE_* entries for removed/renamed modules persist forever.
- **datastore** `modules/datastore/lib.js:115` — write_users/write_containers gate writes on process.env.SC_DATASTORE_RO, which is never exported anywhere in the repo (bash uses the unexported SC_DATASTORE_RO_USE); the JS readonly guard never fires, so put/new/del/add/cfg silently write JSON into the readonly datastore dir and only the git commit is skipped.
- **datastore** `modules/datastore/main.js:157` — Two separate `if (CMD === ADD)` blocks: `add container X vncuser Y` calls write_containers() in the first block before the vncuser is pushed, then pushes and writes again in the second block, producing two racing async fs.writeFile on the same file with nondeterministic persisted state (lost-update).
- **default** `modules/default/server.js:6` — Unguarded readFileSync of /var/www/html/404.html at startup: if the file is missing the process throws before listen(), and with Restart=always plus no RestartSec (hooks/update-install-host.sh:13) systemd crash-loops into permanent failed state, so all unmatched-Host traffic gets haproxy's raw 503 instead of the branded 404.
- **dns** `modules/dns/dns-scan.js:93-96` — A/AAAA/MX/NS arrays are wiped to [] before the hourly-skip check (lines 105-109) and before async resolution completes, so a transient resolver failure (or a skipped problematic domain) persists empty records to containers.json, causing letsencrypt (letsencrypt.js:284-292) to refuse issuance/renewal with "no A record" for domains whose DNS is actually fine.
- **dns** `modules/dns/dns-scan.js:155-157` — The exit handler unconditionally writeFileSync's the entire in-memory containers object over $SC_DATASTORE_DIR/containers.json with no lock and no read-only-datastore check (unlike write_containers in modules/datastore/lib.js:129-137), silently clobbering any concurrent containers.json modification made during the scan window by another srvctl command or another cluster host on the shared gluster datastore.
- **dns** `modules/dns/dns-scan.js:126-139` — AAAA/MX/NS lookups only run inside the resolve4 success callback, so IPv6-only domains (resolve4 → ENODATA) never get AAAA/MX/NS data recorded, and because ENODATA is not in the skip list (lines 106-108) they are fruitlessly re-queried on every run.
- **firewalld** `modules/firewalld/hooks/update-install-ve.sh:5` — Unconditional sc_install call, but sc_install is only defined on Fedora (modules/srvctl/libs/fedoralib.sh:4 returns early otherwise), so inside Debian/Ubuntu/Arch containers the firewalld hook chain ends with firewall-cmd exiting 127 and run_hook's exif (commonlib.sh:104) aborts sc update-install before completion.
- **firewalld** `modules/firewalld/hooks/firewalld.sh:21` — Unconditionally opens ports 8080, 8443 and Elasticsearch 9200 (plus all mail ports at lines 10-14) on every managed host and container regardless of whether those services run, needlessly enlarging the attack surface (e.g., unauthenticated Elasticsearch reachable on 9200).
- **ftp** `modules/ftp/hooks/update-install-host.sh:3` — vsftpd is installed but never configured/enabled/started (no conf template, no add_service anywhere in the repo) even though documentation.md:1229 claims the module 'installs and configures vsftpd', so after update-install port 21 is open in firewalld while no FTP daemon runs and the module delivers no working FTP.
- **gluster** `modules/gluster/module-condition.sh:38` — Final success branch is `echo false` with `#echo true` commented out (since e179f53), so the module can never activate; gluster replication, /var/srvctl3/gluster/* RO mounts and cert renewal are silently off cluster-wide while downstream consumers (datastore RO dir default, ssh pubkey lookup) still point at the now-unmounted paths.
- **gluster** `modules/gluster/libs/glusterlib.sh:141` — gluster_mount_data's missing-brick and missing-/etc/ssl/glusterfs.* error paths (lines 141,148,153,158) end with `err` + bare `return`, which returns exit 0, so datastore/hooks/init.sh:5 treats the failed mount as success and sets SC_DATASTORE_RO_USE=false, pointing the datastore at an unmounted, unreplicated directory.
- **gluster** `modules/gluster/hooks/update-install-host.sh:52` — The CA reachability probe (line 40) disables ssh host-key checking but the actual rsync transfers (lines 52,58,64) use default ssh settings, so on a fresh host with no known_hosts entry for the CA an unattended update-install blocks on an interactive host-key prompt (and the probe itself is MITM-able while fetching CA material).
- **gui** `modules/gui/server.js:189` — socket.request.headers.referer.split('/') with no guard: a socket.io connection lacking a Referer header throws an uncaught TypeError in an async callback and crashes the whole daemon for all users.
- **gui** `modules/gui/server.js:139` — `if (err) throw err;` inside the async conn.exec callback turns any ssh exec-channel failure into an uncaught exception that kills the server process.
- **gui** `modules/gui/server.js:128` — Container selection sets user='root' and host from client-supplied cmd.container with no ownership validation against the datastore, letting any authenticated user attempt root ssh to arbitrary hosts/containers (gated only by remote key acceptance).
- **gui** `modules/gui/libs/spec.sh:30` — Command scan iterates all $SC_MODULES without checking SC_USE_* enablement (unlike lines 49-62), so disabled modules' commands appear as GUI buttons that fail when clicked.
- **gui** `modules/gui/server.js:12` — SC_DATASTORE_DIR and SC_INSTALL_DIR are hardcoded (TODO acknowledged at line 11); non-default install paths break datastore reads and the category parser at line 80.
- **haproxy** `modules/haproxy/libs/proxylib.sh:9` — load_certificate_folder_files calls check_pem (certificates/libs/domaincertlib.sh:34) which rm -rf's any source cert expiring within 7 days — hourly cron regeneration silently deletes still-valid admin-managed certs from /etc/srvctl/cert while the stale copy left in /var/haproxy keeps being served.
- **haproxy** `modules/haproxy/libs/systemdlib.sh:25` — reload_haproxy ignores a failed 'systemctl reload' when the old process stays active (e.g. invalid generated config): it prints 'haproxy.service active' and returns success, so the new config is silently never applied and no syntax check runs.
- **letsencrypt** `modules/letsencrypt/letsencrypt.js:298` — Operator-precedence bug in the nameserver diagnostic string: plus binds tighter than logical-or, so a truthy NS[0] hides NS[1] and the intended per-element fallback never happens; the no-A-record error prints garbled or incomplete nameserver info.
- **letsencrypt** `modules/letsencrypt/apps/acme-server.js:15` — fs.access(file, fs.R_OK, cb) uses the deprecated fs.R_OK alias; where it is undefined the check degrades to F_OK (existence not readability), so an unreadable challenge file is mishandled and behavior is Node-version fragile.
- **mariadb** `modules/mariadb/libs/mariadblib.sh:55` — backup_mariadb deletes all previous dumps (rm -fr /root/mariadb-dump/*) before producing new ones, so a mid-loop mysqldump failure (exif aborts at :75) leaves zero/partial dumps for the outer file backup to archive.
- **mariadb** `modules/mariadb/libs/mariadblib.sh:159` — secure_mariadb's UPDATE mysql.user SET Password=PASSWORD(...) (and the unchecked DELETEs at :153-157) fail on MariaDB >=10.4 where mysql.user is a non-updatable view, so the server can never be secured on the Fedora versions this targets.
- **mariadb** `modules/mariadb/libs/mariadblib.sh:129` — /etc/mariadb-<db>.conf (and /etc/mysqldump.conf at :168-173) are written with default umask (0644) containing plaintext DB passwords, readable by any unprivileged user in the container.
- **mariadb** `modules/mariadb/libs/mariadblib.sh:102` — db user truncated to 15 chars: two site names sharing the first 15 sanitized chars collide, and the second add_mariadb_db's GRANT ... IDENTIFIED BY resets the first user's password, breaking the existing site's DB access.
- **mozilla** `modules/mozilla/apps/mozilla-autoconfig-server.js:57` — Server binds 0.0.0.0:1029 and (per libs/install.sh:15-16) runs as root although its only consumer is haproxy at 127.0.0.1:1029, leaving an unnecessary root-owned listener reachable on mesh/LAN interfaces the firewall zone permits.
- **named** `modules/named/named.js:382-394` — The process.on('exit') handler sets process.exitCode=0 after return_error's process.exit(111) (Node re-reads exitCode after 'exit' listeners), so e.g. 'could not locate master servers' (named.js:101) still writes a broken srvctl.conf with empty 'masters {};' and exits 0 — namedcfg's exif passes and restart_named restarts BIND on an unloadable config; if clusters.json is unreadable the handler instead crashes with a TypeError on undefined clusters.
- **named** `modules/named/named.js:322-330` — Peer HTTPS fetch has no effective timeout (Agent timeout:1000 has no 'timeout' listener and the request is never destroyed), so one unresponsive cluster host can stall named.js and therefore 'sc regenerate' indefinitely.
- **named** `modules/named/libs/install.sh:11-14` — install_named installs/enables ntp/ntpd, retired on modern Fedora (chrony), so install emits errors and configures no time sync; likewise installnamedlib.sh:51 uses the obsolete bindkeys-file option and install.sh:28-29 rsyncs /usr/share/doc/bind/sample paths recent bind packages no longer ship, with failures unchecked.
- **named** `modules/named/installnamedlib.sh:18` — Dormant dyndns-server.service ExecStart points at modules/named/hs-apps/dyndns-server.js but the file lives at modules/named/apps/dyndns-server.js, so the unit could never start.
- **nfs** `modules/nfs/libs/nfslib.sh:17` — nfs_mount iterates the full cluster host_list with no self-skip, so each host NFS-loopback-mounts its own /srv at /var/srvctl3/nfs/$HOSTNAME/srv, a mount no consumer uses (usersonhost/main.js only reads it for remote hosts) and which is deadlock-prone under memory pressure.
- **nfs** `modules/nfs/libs/nfslib.sh:27` — mount is issued with no already-mounted check, and the regenerate hook fires on every sc regenerate/add-ve/add-ve-user/add-network-ve, so duplicate NFS mounts stack up (or spurious eyif errors print) on every run.
- **nfs** `modules/nfs/hooks/update-install-host.sh:3` — The 'Install NFS' hook never installs the nfs-utils package (no reference anywhere in the repo), so on a minimal Fedora host showmount/mount.nfs/nfs-server.service are absent and the module writes an export it can never serve while add_service errors 'No such service - nfs-server'.
- **ntp** `modules/ntp/hooks/update-install-host.sh:6` — The hook ends with `run systemctl start ntpd`, whose nonzero exit code becomes the sourced hook's return status and trips exif in run_hook (commonlib.sh:104), aborting the entire update-install run — silently at origin, since run suppresses its eyif warning for systemctl commands (lablib.sh:108) — so all alphabetically-later modules' host hooks, completion install, and set_permissions are skipped whenever ntpsec fails to install or ntpd fails to start.
- **odoo** `modules/odoo/commands/install-odoo.sh:319` — Blind `sed -i -e '218d'` deletes a hard-coded line number of /etc/httpd/conf.d/ssl.conf assumed to be </VirtualHost>; any packaging shift or prior local edit corrupts the config after the append at 320-329 and httpd fails to start, taking down every site in the container.
- **odoo** `modules/odoo/commands/install-odoo.sh:226` — `chown -R odoo:odoo /srv/*` (also line 290) seizes ownership of everything under the container's /srv, silently breaking permissions of any co-hosted application data.
- **odoo** `modules/odoo/commands/install-odoo.sh:161` — Unconditional `dnf update -y` performs a full package upgrade of the production container as a hidden side effect of an install command.
- **odoo** `modules/odoo/commands/install-odoo.sh:299` — Pinned to EOL Odoo/OCB 14.0 whose requirements.txt no longer builds on current Fedora Python (3.12+), so pip install fails (silently, per the errexit bug) and the module cannot produce a working install today.
- **odoo** `modules/odoo/commands/install-odoo.sh:313-330` — httpd and mod_ssl are assumed preinstalled but never installed (not in the dnf list at 162), yet the script edits /etc/httpd/conf.d/ssl.conf, writes /var/www/html, and enables/restarts httpd — on a container without them the HTTPS proxy setup silently fails.
- **opendkim** `modules/opendkim/opendkim.js:105` — txt.split('"')[3] captures only the first quoted chunk of opendkim-genkey's TXT output, so multi-chunk keys (e.g. -b 2048) are stored truncated and named publishes a broken DKIM record; if the format shifts, [3] is undefined and line 106 silently deletes a previously valid dkim-*-domainkey from containers.json.
- **opendkim** `modules/opendkim/opendkim.js:130` — containers.json is rewritten wholesale on every regenerate from a snapshot loaded at process start with no locking or change detection, silently reverting concurrent datastore changes (e.g. a parallel add-ve) and churning the shared datastore even when nothing changed.
- **openvpn** `modules/openvpn/hooks/update-install-host.sh:89` — The repair symlink for hostnet-ccd can never be created: `[[ ! -f dir ]]` is always true for a directory and `ln -s` always fails EEXIST against the real directory made by openvpnconfiglib.sh:12 (verified), leaving the empty-ccd bug unmitigated plus per-host error noise; the block also sits inside the peer loop so single-host clusters never attempt it.
- **openvpn** `modules/openvpn/libs/openvpnlib.sh:65` — Certificates are rsynced from the CA host only when the local file is missing (also lines 71 and 78), so expired or renewed certs are never re-fetched on non-CA hosts and the mesh stays down after expiry until the local pem files are deleted by hand.
- **openvpn** `modules/openvpn/hooks/update-install-host.sh:84` — When write_openvpn_client_config bails because host_ip/hostnet is missing from the datastore, the hook still creates a dangling conf symlink and enables the unit (line 85), leaving a permanently failing enabled openvpn-client service on every boot.
- **openvpn** `modules/openvpn/hooks/update-install-host.sh:51` — `ln -s` without -f or existence guard (also line 84) fails with 'File exists' on every update-install re-run; line 51 is not run-wrapped so it fails with only raw stderr — idempotency noise on repeated installs.
- **password** `modules/password/libs/bashlib.sh:8` — 2>&1 merges node stderr into the captured password, so any node warning that exits 0 (e.g. NODE_OPTIONS or deprecation warnings) silently corrupts the credential written into DB grants, user accounts, or SSL configs.
- **perdition** `modules/perdition/hooks/version.sh:3` — msg_version_installed Pound queries the wrong package (leftover from the retired pound module) so perdition's version is never reported; additionally no run_hook version exists anywhere, making the hook dead code.
- **perdition** `modules/perdition/conf/perdition.conf:36` — no_lookup per perdition(8) disables map lookup and relays all users to outgoing_server localhost, contradicting the map_library popmap machinery (conf:32-33) the whole module exists to feed — either the generated popmap is silently ignored at runtime or the option is dead weight; must be verified on a live host before rewriting.
- **postfix** `modules/postfix/conf/ve-master.cf:1` — Template is referenced by no code in the repo, so containers keep the stock Fedora master.cf and never get the submissions/465 listener it defines (lines 32-47), while hooks/mkrootfs_fedora.sh:5-6 opens smtps(465) in the mail image firewall — SMTPS submission to mail containers silently cannot work.
- **postfix** `modules/postfix/diagnose.sh:1` — File sits at module root but run_hook only sources hooks/<name>.sh (commonlib.sh:97), so 'sc diagnose' (modules/srvctl/commands/diagnose.sh:91) never runs it and postfix TLS/amavis diagnostics are silently missing; moreover line 3's 'openssl s_client' lacks </dev/null and would hang diagnose if the file were relocated as-is.
- **postfix** `modules/postfix/libs/systemdlib.sh:17` — On restart failure restart_postfix ends with 'systemctl status' (exit 3), which propagates via hooks/regenerate.sh:5 and run_hook's exif (commonlib.sh:104) to abort the entire invoking command — a broken postfix kills add-ve/add-ve-user/add-codepad/regenerate mid-flow, leaving half-built containers.
- **saslauthd** `modules/saslauthd/commands/testsaslauthd.sh:31` — When no .password file is found the script only echoes 'Mission failed.' without exiting and still runs testsaslauthd with an empty password (and empty user if ARG missing, no argument guard), producing a pointless real auth attempt and a misleading exit status.
- **saslauthd** `modules/saslauthd/commands/testsaslauthd.sh:36` — Plaintext mailbox password is passed as a CLI argument through the run helper, which prints the full command to the terminal and exposes the secret in the process list.
- **saslauthd** `modules/saslauthd/hooks/update-install-host.sh:25` — Custom unit is written to RPM-owned /usr/lib/systemd/system/saslauthd.service, so any dnf update of cyrus-sasl silently reverts it until the next update-install.
- **saslauthd** `modules/saslauthd/hooks/update-install-host.sh:29` — systemctl daemon-reload runs after add_service has already enabled and restarted the service, so the first restart after a unit-file change can execute the stale in-memory unit definition.
- **srvctl** `modules/srvctl/commands/fix-owner.sh:8` — The command's only action (recursive chown to parent owner) is commented out, so `sc fix-owner` silently does nothing while its help promises the chown.
- **srvctl** `modules/srvctl/command.sh:58` — `[[ "$ck" == "$i" ]]` compares a basename to a full path (never true; intended $service), so an inactive unit named with its explicit suffix (e.g. `sc foo.timer start`) is never matched and falls through to 'Invalid command.' (same defect at line 74 for user units).
- **srvctl** `modules/srvctl/command.sh:90` — Unconditional `exit_0` after service_action makes every failure (non-root return 66, unknown op return 223, systemctl errors) exit 0, so automation cannot detect a failed `sc SERVICE start`.
- **srvctl** `modules/srvctl/commands/version.sh:9` — `sc version` never calls `run_hook version`, so the version hooks shipped by named/postfix/perdition/opendkim are dead code and the command reports only postfix and nodejs.
- **srvctl** `modules/srvctl/libs/completionlib.sh:11` — `chmod 777 /var/local/srvctl/completion` lets any local user replace any other user's (incl. root's) .hints/.commands/.units files, which are cat'ed into every interactive shell at login and fed to compgen.
- **srvctl** `modules/srvctl/completion.sh:58` — `arr=("$command")` builds a one-element array so `argument=${arr[$length-2]}` is always empty for positions >=1, meaning per-argument tab completion never resolves (and the grep at :57 is an unanchored substring match).
- **ssh** `modules/ssh/sshd_authorization.sh:7` — share/common/authorized_keys is emitted for every authenticating user (%u ignored), letting any admin key log in as any local account on the host, contradicting the 'used by root as root' comment.
- **ssh** `modules/ssh/ssh.js:216-224` — When ssh-keyscan succeeds with empty output (stopped container), split(" ")[2] is undefined and fs.writeFileSync(path, undefined) throws, printing a stack trace per stopped container on every regenerate; check_host_keys also stores undefined and forces a needless hosts.json rewrite.
- **ssh** `modules/ssh/ssh.js:206-227` — /srv/<C>/host_key is a self-written cache never re-validated against the container's real key (create-container.sh:56 deletion is commented out), so rebuilt containers keep stale entries in the shared known_hosts forever.
- **sshpiperd** `modules/sshpiperd/hooks/update-install-host.sh:28-29` — The systemd unit is installed and daemon-reload run but the service is never enabled or started, so a fresh update-install leaves sshpiperd dead until an operator manually runs 'sc sshpiperd !'.
- **sshpiperd** `modules/sshpiperd/workingdir.go:192-196` — Any ssh.ParseAuthorizedKey error (one malformed line in any <user>/*.pub or the shared authorized_keys file) aborts the whole key scan and denies auth for all keys appearing after the bad line.
- **static** `modules/static/hooks/update-install-host.sh:38-39` — The service is enabled/started on every host but nothing in the codebase routes to port 1280 (haproxy default backend is localhost:1282, modules/haproxy/haproxy.js:513; the pound proxy referenced in server.js:5 is gone), leaving a permanently running root HTTP server with the above vulnerabilities on all interfaces while providing no function.
- **usersonhost** `modules/usersonhost/commands/add-user.sh:16` — Username regex `[a-z_][a-z0-9_]{2,30}` is unanchored (no ^…$), so strings merely containing a valid substring (e.g. '!!!abc', names with spaces) pass validation and flow into `new user` and later `adduser`; same defect in add-reseller.sh:16.
- **usersonhost** `modules/usersonhost/main.js:263` — `adduser -U -c '"+users[u].name+"' …` interpolates the datastore-supplied name/reseller into a single-quoted shell string with no escaping, so a name containing a single quote yields shell command injection running as root at regenerate time.
- **usersonhost** `modules/usersonhost/libs/userlib.sh:60` — `regenerate_users()` calls `userscfg` then `return`, leaving the per-user loop plus `create_user_id` and bash `crate_user_password` as unreachable dead code that duplicates and can drift from main.js.
- **usersonhost** `modules/usersonhost/libs/bashlib.sh:10` — `usercfg` (and thus user.js) has no caller anywhere in the repo — dead code; if re-enabled, user.js:43 `Object.keys(users[SC_USER])` throws for any SC_USER absent from users.json.
- **usersonve** `modules/usersonve/commands/install-qlcplus.sh:60` — Source path typo 'qlcpuls' (listing dir on line 57 is 'qlcplus') makes every cat fail while the > redirection still creates the target, so fixture sync is completely broken and leaves empty fixture files in /home/x/.qlcplus/fixtures/.
- **usersonve** `modules/usersonve/commands/install-qlcplus.sh:38` — '[[ /home/x/autostart.sh ]]' is a non-empty-string test (missing -f) that is always true, so /home/x/autostart.sh is unconditionally overwritten, clobbering the CrossOver kiosk autostart written by install-crossover.
- **usersonve** `modules/usersonve/commands/install-qlcplus.sh:54` — mkdir -p /home/x/.qlcplus/fixtures as root without chown leaves QLC+'s config dir root-owned, so the app running as user x cannot save its configuration or workspaces.
- **usersonve** `modules/usersonve/commands/add-user.sh:13` — Username validation regex '([a-z_][a-z0-9_]{0,30})' is unanchored, so any input containing one valid character run (e.g. 'bad!name') passes and is fed to adduser.
- **usersonve** `modules/usersonve/commands/add-user.sh:40` — When adduser fails, home is empty so the plaintext password is written to /.password at filesystem root, passwd failure is invisible (stdout+stderr discarded), and line 38 still prints a password that was never set.
- **usersonve** `modules/usersonve/commands/add-zerotier.sh:41` — USER WIP file (excluded from rewrite, present in HEAD too): line ends with '&& \' plus two trailing spaces, so the backslash escapes a space instead of the newline; the command after && is a literal-space word that fails with 'command not found' (127) and the GPG key-import gate never gates the installer on line 42.
- **ve** `modules/ve/commands/status.sh:9` — Unguarded `du -hs /home; du -hs /srv; du -hs /var` as the command body: for non-root users du hits permission-denied subdirs and exits 1 as the script's last command, so run_command's exif (commonlib.sh:165) prints "'status' failed" and srvctl exits 1 (plus undercounted sizes and stderr noise) even though the informational command succeeded.
- **vncproxy** `modules/vncproxy/start.sh:37` — [[ /bin/vncproxy ]] tests a non-empty string literal (always true) instead of file existence, so on hosts where build.sh never ran the service crash-loops every 3s on 'command not found' while still echoing a misleading 'OK: vncproxy ...:5900'.
- **vncproxy** `modules/vncproxy/vncproxy.js:69` — VNC passwords are a deterministic non-cryptographic 8-char hash of the public container name and username with no salt, so anyone with the open-source algorithm can compute any user's VNC password offline.
- **vncproxy** `modules/vncproxy/vncproxy.js:101` — /var/vncproxy (0755) plus records and vncproxy.db written with default 0644 make all plaintext VNC forward keys readable by every local user on the host.
- **vncproxy** `modules/vncproxy/services/vncproxy.service:1` — No hook or command installs the service unit, builds the binary, or installs the restarter timer (zero references outside the module), so add-vnc-user's 'systemctl restart vncproxy' fails on any freshly provisioned host — the module is not self-installing.
- **vncproxy** `modules/vncproxy/vncproxy-restarter.sh:5` — Watchdog depends on nmap which nothing installs (dnf.sh only installs sqlite); with the timer active and nmap missing the FAIL branch restarts vncproxy every minute, killing all live VNC sessions.
- **wordpress** `modules/wordpress/scripts/restore-wordpress-password.sh:41` — All failure paths use bare `exit` right after a successful echo (also :9,52,58,66,74,84), so the script exits 0 on failure and automation cannot detect that no password was restored.
- **wordpress** `modules/wordpress/commands/install-wordpress.sh:55` — `run curl ... > latest.zip` prepends run's ANSI command banner inside the zip, and download failure never aborts (run/eyif interplay never even warns), so the install proceeds through DB creation and prints the success message over a broken/empty webroot.
- **wordpress** `modules/wordpress/commands/install-wordpress.sh:130` — $URI is defined nowhere, so WP_SITEURL is `http://$HOSTNAME/` while FORCE_SSL_ADMIN is true (:114) and the success message says https — behind the TLS-terminating proxy with no X-Forwarded-Proto handling, /wp-admin redirect-loops and front-end links are http.
- **wordpress** `modules/wordpress/commands/install-wordpress.sh:160` — wp-install.php (containing the plaintext admin password, :139, never deleted) plus .admin and wp-config.php are chown -R apache:apache after the chmod 000 (:150-151), so a compromised apache-uid process owns the credential files and can chmod/read or rewrite them.

## LOW (151)

- **backup** `modules/backup/libs/sclib.sh:190` — Failure log lines in server_backup and remote_backup (also :290) are labeled `local-backup-failure` (copy-paste of :92), making remote failures indistinguishable from local ones in backup.log.
- **backup** `modules/backup/libs/sclib.sh:33` — `for i in $args` (also :128, :226) word-splits and glob-expands directory arguments, so paths with spaces/globs back up wrong locations; combined with rsync --delete-after a wrong-but-existing target can mirror-delete unrelated backup data.
- **backup** `modules/backup/libs/sclib.sh:279` — remote_backup probes reachability as plain $host (:230) but rsyncs as root@$host, so when the proxy's ssh config maps the host to a different user the probe passes and the actual transfer fails auth (or vice versa).
- **backup** `modules/backup/libs/7zlib.sh:42` — Dead today but live if line 28 is removed: `run ssh ... > $to/packagelist` redirects the run helper's ANSI-colored command-echo line into packagelist, corrupting the package inventory file.
- **backupdb** `modules/backupdb/libs/backupdblib.sh:28` — out_path is not declared local and leaks a global variable into the sourcing srvctl process.
- **backupdb** `modules/backupdb/module-condition.sh:3` — Condition is an unconditional 'echo true', so the module loads on every machine including ones with no databases and SC_USE_BACKUPDB carries no signal.
- **branding** `modules/branding/hooks/update-install-host.sh:11` — Typo "apgain" in the 500 error text is baked into /var/www/html/500.html and haproxy's 500.http, so end users see a misspelled error page on every internal server error.
- **branding** `modules/branding/libs/brandinglib.sh:24` — Literal double-quote characters inside the unquoted heredoc (around the inlined logo.svg at line 24 and around "$_name @ $HOSTNAME" at line 29) are emitted into the HTML, so every generated default index page renders stray quote marks.
- **branding** `modules/branding/hooks/pre-init.sh:9` — SC_RESELLER_USER is defaulted but never exported (post-init.sh exports only SC_COMPANY/SC_COMPANY_DOMAIN), so its sole consumer modules/datastore/main.js:34 (process.env.SC_RESELLER_USER) always sees undefined — dead plumbing that would silently break any reseller check built on it.
- **branding** `modules/branding/html/503.html:1` — Orphaned static asset referenced by no code and drifted from the generated pages (missing the trailing IPHUTHA! language entry), inviting edits to the wrong file during maintenance.
- **ca** `modules/ca/libs/calib.sh:63` — Cleanup hardcodes rm -fr /etc/srvctl/CA/tmp/* instead of $SC_ROOTCA_DIR/tmp/*, so with a customized SC_ROOTCA_DIR the CSR temp files (private-key requests) are never cleaned; ca_sync (netlib.sh:12) hardcodes the same path so a custom dir also breaks replication.
- **ca** `modules/ca/libs/calib.sh:46` — root_CA_init does not validate its argument and certificates/hooks/update-install-host.sh:35 calls it with none, creating junk hidden CA material ($SC_ROOTCA_DIR/ca/.key.pem, .crt.pem with CN '$SC_COMPANY--ca', .srl) on the CA host.
- **ca** `modules/ca/libs/calib.sh:59` — chmod -R 600 strips execute bits from all CA directories (non-traversable for non-root), while later-created .crt.pem/.srl/.csr.pem/.p12 files (the p12 contains a private key) get default-umask 644 and are protected only by the 600 parent directory.
- **ca** `modules/ca/libs/netlib.sh:10` — Reachability check string-compares `ssh $SC_ROOTCA_HOST hostname` output to $SC_ROOTCA_HOST, so a short-name vs FQDN mismatch produces a false 'could not be reached' error and CA sync is silently skipped.
- **ca** `modules/ca/module-condition.sh:3` — The [[ -d /etc/openvpn ]] check precedes the containers-condition guard, so the module and its hooks activate inside nspawn containers that have the openvpn package installed.
- **certificates** `modules/certificates/libs/domaincertlib.sh:3` — check_pem is annotated '## unused' but is load-bearing for haproxy (proxylib.sh:9) and returns 0 unconditionally, so the annotation invites deletion that would break haproxy cert pruning.
- **certificates** `modules/certificates/hooks/update-install-host.sh:35` — root_CA_init is called with no argument, creating a nameless CA (/etc/srvctl/CA/ca/.key.pem etc., hidden 4096-bit key) that no create_ca_certificate caller ever references — wasted keygen and a stray unmanaged private key.
- **certificates** `modules/certificates/libs/wildcardcertlib.sh:55` — $SC_DATASTORE_DIR/cert is never created by this module (default RO datastore path, and haproxy's mkdir runs after this hook in its regenerate chain), so on first run every cat fails and wildcard certs are silently not applied.
- **certificates** `modules/certificates/hooks/update-install-host.sh:38` — install_acme runs here and again from letsencrypt's own update-install-host hook (module order is alphabetical), so every update-install performs the acme installation twice (useradd error noise, double service enable/start).
- **certificates** `modules/certificates/libs/domaincertlib.sh:184` — The throwaway key passphrase is echoed to the terminal by run on openssl command lines (and ps-visible argv) and persisted in config.txt (output_password, :154) left in the cert directory.
- **codepad** `modules/codepad/libs/mkrootfs_fedora_install_codepad.sh:52` — `email = codepad@$CDN` references $CDN which is defined nowhere in the repo or config examples, producing the malformed git identity 'codepad@'.
- **codepad** `modules/codepad/libs/mkrootfs_fedora_install_codepad.sh:38` — Creates a mongod.service wants-symlink although no mongodb package is in the install list (line 10), leaving a dangling symlink that logs unit-load errors at every container boot and no running mongo.
- **codepad** `modules/codepad/libs/mkrootfs_fedora_install_codepad.sh:45` — `run cd "$rootfs"/var/git` changes the srvctl process CWD and never restores it, so all subsequent hooks in the same run (e.g. the containers module's fedora rootfs build) execute from inside the codepad template.
- **codepad** `modules/codepad/libs/mkrootfs_fedora_install_codepad.sh:33` — `echo '# Init\n'` writes a literal backslash-n into project.log (single quotes, no -e).
- **codepad** `modules/codepad/commands/add-codepad.sh:34` — run_hook add_codepad_project is executed twice per add-codepad (also inside init_codepad_project at libs/init_codepad_project.sh:34); latent double-execution once any module ships that hook.
- **codepad** `modules/codepad/libs/create_codepad_certificate.sh:20` — Heredoc appends (>>) to localhost-cert-config.txt; if the key exists but the crt is missing from a partial run, the guard re-enters and the openssl config accumulates duplicate sections.
- **codepad** `modules/codepad/access.js:83-86` — Creates empty $SC_DATASTORE_DIR/users/<u> directories as a side effect for any referenced user without one, littering the datastore and writing into the read-only replica dir on secondary hosts.
- **containers** `modules/containers/libs/backupcontainerlib.sh:21` — run performs no shell redirection, so '>' and the path are passed to dnf as package specs; dnf errors and dnf.packages.list is never written into the backup.
- **containers** `modules/containers/libs/addcontainerlib.sh:61` — 'run cat X > Y' redirects run's ANSI command banner into the target, prepending garbage to /var/srvctl3/datastore/cert/$C.pem and (line 69) the container's /etc/systemd/resolved.conf.
- **containers** `modules/containers/libs/statuslib.sh:106` — bash_containers_status calls undefined function container_status (actual name bash_container_status); currently dead code but breaks if invoked.
- **containers** `modules/containers/commands/destroy-ve.sh:73-81` — Unbounded 'while [[ -d /srv/$C ]]; do rm -fr' loop (same in remove-ve.sh:89-97) hangs forever on busy/NFS-stale mounts inside /srv/$C.
- **containers** `modules/containers/commands/recreate-ve.sh:125` — rm -fr srv/"$C"/... lacks the leading slash, so isolate-*.log files are never removed from the rebuilt rootfs.
- **containers** `modules/containers/libs/mkrootfs_arch.sh:49` — Arch (and ubuntu at mkrootfs_ubuntu.sh:45) rootfs builds run 'run_hooks mkrootfs_debian' — copy-paste hook name gives arch images debian hooks and no arch-specific ones.
- **containers** `modules/containers/libs/systemlib.sh:139` — create_srvctl_nspawn_service checks /usr/lib/systemd/system for the unit but writes /etc/systemd/system, so the idempotence check never matches and every update-install clobbers any admin-customized unit file.
- **containers** `modules/containers/command.sh:11` — GUI spec comment maps the 'show' action to 'poweroff VE'; if the external GUI executes these spec lines, viewing a container powers it off.
- **containers** `modules/containers/libs/all_containers_quota_check.sh:27` — If 'get container quota' fails, empty quota is treated as 0 in the arithmetic comparison and a healthy container is disabled and stopped by the hourly cron.
- **core** `commonlib.sh:352` — The full 'sc help' listing iterates SC_MODULES without SC_USE_* enablement or root_only/hs_only/reseller_only filtering, documenting disabled-module and root-only commands to ordinary users, inconsistent with hint_commands.
- **core** `commonlib.sh:104` — run_hook calls exif after sourcing each hook, so a hook whose last statement is a benign failed conditional aborts the entire CLI with "hook failed" and that conditional's exit code.
- **core** `srvctl.sh:107` — source init.sh || echo continues execution after a failed init, producing confusing command-not-found cascades instead of a clean abort.
- **core** `init.sh:19` — For a non-root user on a host missing the /bin/sc, /bin/srvctl or completion symlinks, every invocation runs sudo ln -s, which can hang on a password prompt or spam sudo denials.
- **core** `srvctl.sh:30` — If /bin/pop exists, every root TTY invocation executes it before dispatch (dev auto-resync hook), silently rewriting the installation at the start of unrelated commands.
- **core** `lablib.js:51` — get()'s catch dereferences e.stderr.toString() without the undefined guard used in run(), so a spawn-level failure throws a TypeError inside the error handler, masking the original error.
- **core** `commonlib.sh:324` — help_on_file greps all '## @en' matches without -m1 and strips the 7-char marker only from the first, so files with multiple @en lines render raw '## @en' markers in the help output.
- **datastore** `modules/datastore/lib.js:464` — container_add_mapped_port reads process.argv[7] into port_arg then calls port_arg.indexOf(':'); invoking cfg add_mapped_port without a port makes port_arg undefined and throws an uncaught TypeError instead of a clean return_error.
- **datastore** `modules/datastore/apps/datastore-server.js:34` — pki-validation handler builds the file path by concatenating req.url.substring(28) with no sanitization, allowing path traversal (../) to read files outside /var/srvctl3/datastore/pki-validation on this internet-reachable ACME endpoint.
- **datastore** `modules/datastore/libs/bashlib.sh:90` — del and add declare `local __result` but never assign it (node output goes straight to stdout), so the exif error diagnostic always omits the node error text that new/get/put/out capture via 2>&1, hiding failure detail.
- **datastore** `modules/datastore/lib.js:857` — new_container has a stray `console.log(C, T, B)` debug print that fires on every container creation.
- **default** `modules/default/server.js:20` — server.listen(1282) binds all interfaces instead of 127.0.0.1 though the only client is haproxy on localhost; exposure is prevented only by firewalld not opening the port, and it is reachable over any permissive zone/interface such as the 10.x VPN mesh.
- **default** `modules/default/hooks/update-install-host.sh:22` — Uses systemctl start (not restart), so re-running update-install after a srvctl code update leaves an already-running daemon executing stale server.js until reboot.
- **default** `modules/default/hooks/update-install-host.sh:21` — Failures of systemctl enable/start are silently swallowed because run() in lablib.sh:108 skips the eyif warning for any command starting with 'systemctl', so a broken unit install produces no error in the update log.
- **dns** `modules/dns/dns-scan.js:63` — Resolver is hardcoded to the single upstream 8.8.8.8 with no config knob, bypassing the system resolver and the named module; where Google DNS is unreachable every domain flips to ETIMEOUT, records get wiped, and letsencrypt is blocked cluster-wide.
- **firewalld** `modules/firewalld/hooks/diagnose.sh:7` — Uses $zone that the hook never sets (own declaration commented out at line 3), relying on the variable leaked from modules/srvctl/commands/diagnose.sh:73; if /usr/sbin/firewalld is absent or the parent command is reordered, it runs `firewall-cmd --zone= --list-services` and prints errors.
- **firewalld** `modules/firewalld/libs/firewalldlib.sh:34` — When called for a service with no built-in XML and empty proto/port, writes invalid `<port protocol="" port=""/>` to /etc/firewalld/services/$name.xml and reloads; the broken file persists and can make every subsequent firewall-cmd --reload on the host error until manually removed.
- **firewalld** `modules/firewalld/libs/firewalldlib.sh:12` — hint is missing from the local declaration at line 6, leaking a global `hint` variable into the shell that sources all subsequent hooks (benign today only because commonlib's hint() declares its own local).
- **firewalld** `modules/firewalld/libs/firewalldlib.sh:1` — Malformed shebang `#!bin/bash` (missing leading slash); harmless while sourced but breaks direct execution and tooling shebang detection.
- **ftp** `modules/ftp/hooks/firewalld.sh:3` — Plaintext-FTP port 21 is permanently opened in the default zone on every container-farm host at every update-install with no opt-out short of disabling the module, a standing firewall exposure for a service that is not even running.
- **ftp** `modules/ftp/hooks/update-install-host.sh:3` — The hook's exit status is dnf's, so a transient dnf/repo failure installing the nonessential vsftpd package makes run_hook's exif (commonlib.sh:104) abort the entire `sc update-install` run mid-way.
- **gluster** `modules/gluster/libs/glusterlib.sh:7` — gluster_reset runs `umount /srvctl/data`, a stale hardcoded path (the real mountpoint is $SC_DATASTORE_RW_DIR=/var/srvctl3/datastore), so the volume is stopped and deleted while still fuse-mounted.
- **gluster** `modules/gluster/libs/glusterlib.sh:12` — gluster_reset runs `attr -R -r glusterfs.volume-id .` against the caller's current working directory instead of the brick path, so the intended xattr cleanup does not happen where intended.
- **gluster** `modules/gluster/libs/glusterlib.sh:162` — Offline-brick detection greps the exact column spacing 'N/A       N/A        N       N/A' from `gluster volume status` output; any gluster CLI format change silently disables the force-restart of offline bricks.
- **gluster** `modules/gluster/libs/glusterlib.sh:48` — gluster_configure reports 'gluster inactive' with err but returns 0, so datastore/static update-install-host hooks proceed as if the volume were configured.
- **gluster** `modules/gluster/hooks/update-install-host.sh:4` — Documented invocation `sc exec-function run_module_hook gluster update-install` is broken twice over: run_module_hook was removed (commented out at commonlib.sh:69-83) and the hook was renamed to update-install-host.
- **gui** `modules/gui/libs/spec.sh:11` — `head "$1" | grep -m 1 "$HEMP" "$1"` passes a file operand so grep ignores the head pipe and matches '## @@@' anywhere in the file, turning stray occurrences (heredocs, help text) into bogus GUI argument specs.
- **gui** `modules/gui/libs/spec.sh:10` — Hint grep lacks -m 1; a command file with two '## @en' lines in its head would embed a newline in commands.spec, producing a malformed record that makes server.js:84 (la[3].split) throw at startup — latent daemon-won't-start (no current file triggers it).
- **gui** `modules/gui/server.js:59` — containers/users/hosts JSON and commands.spec are read once at startup into consts, so new or removed containers/users/commands stay invisible in the GUI until service restart.
- **gui** `modules/gui/server.js:167` — readyTimeout: 500 ms for ssh connections makes commands to slower hosts (e.g. over the OpenVPN mesh) fail with only 'client-timeout error.' shown.
- **gui** `modules/gui/package.json:13` — Declares dead pty.js@^0.3.1 while server.js requires node-pty; installing from this manifest yields a broken dependency set (contradicted by make_node_modules.sh:9).
- **gui** `modules/gui/make_node_modules.sh:7` — Uses the `run` helper but never sources lablib.sh and has no SRVCTL guard, so standalone execution fails every line with 'run: command not found' and installs nothing.
- **gui** `modules/gui/server.js:347` — term2html HTML-escapes only text inside matched SGR color sequences; un-colored output containing &/</> reaches the client raw, garbling display and relying solely on client-side ngSanitize to prevent markup injection.
- **haproxy** `modules/haproxy/libs/proxylib.sh:36` — Cert source path /var/srvctl3/datastore/cert is hardcoded, ignoring SC_DATASTORE_RW_DIR overrides, while line 26 mkdirs $SC_DATASTORE_DIR/cert which may resolve to the read-only gluster dir — certs are missed on hosts with non-default datastore paths.
- **haproxy** `modules/haproxy/commands/https-redirect.sh:28` — Twin commands use different root gates ([[ $USER == root ]] here vs broken [[ $SC_UID0 ]] in http-redirect.sh:29), and both exit 0 on the 'no access' path (exit with no code), so denial is indistinguishable from success for callers.
- **haproxy** `modules/haproxy/haproxy.js:93` — ddn() appends SC_COMPANY_DOMAIN from env without a guard; if unset the generated config contains ACLs for '<host>.undefined' hostnames.
- **letsencrypt** `modules/letsencrypt/libs/letsencryptlib.sh:19` — useradd -r -u 528 acme is unguarded and not wrapped in run/eyif, so re-install emits an unmanaged user-already-exists stderr error.
- **letsencrypt** `modules/letsencrypt/letsencrypt.js:222` — Asymmetric certbot log paths: non-www runs append to /srv/name/letsencryptname.log (no separator) while www runs use /srv/name/letsencrypt-www.name.log, scattering a container's logs across differently named files.
- **letsencrypt** `modules/letsencrypt/letsencrypt.js:131` — check_checkend prints the raw openssl command via ntc() on every call regardless of result and uses return inside a finally block, producing constant log noise and a lint-flagged control-flow pattern.
- **mariadb** `modules/mariadb/libs/mariadblib.sh:165` — MySQL root password is logged in plaintext to $SC_LOG (and echoed to terminal at :109 and :134), persisting secrets in the srvctl log.
- **mariadb** `modules/mariadb/libs/mariadblib.sh:187` — mysql_root passes $1 unquoted to mysql -e, so any query containing a space word-splits and fails (function currently has no callers).
- **mariadb** `modules/mariadb/libs/mariadblib.sh:69` — Database listing filtered with `grep -v Database` skips any database whose name contains the substring 'Database', silently omitting it from backups.
- **mozilla** `modules/mozilla/libs/install.sh:9` — Unit Description is the copy-pasted 'Letsencrypt server.' from letsencryptlib.sh:24, so two different services share a misleading description in systemctl output, confusing operators during triage.
- **mozilla** `modules/mozilla/libs/install.sh:30` — The hook's exit status is that of `run systemctl status`; if the just-started service is not active (e.g. port 1029 conflict, restart backoff -> exit 3), run_hook's exif aborts the entire `sc update-install` mid-run, skipping all later module hooks and set_permissions, with only a generic hook-failed message.
- **mozilla** `modules/mozilla/apps/mozilla-autoconfig-server.js:10` — Provider id is hardcoded to "D250.hu" on every deployment (branding module ignored), so third-party farms serve another organisation's identifier in mail autoconfig.
- **mozilla** `modules/mozilla/apps/mozilla-autoconfig-server.js:47` — Every method and path receives 200 + the full config XML (no path check for config-v1.1.xml), so any scanner hitting :1029 harvests hostname/mail topology and any URL under the haproxy-routed prefix appears to exist.
- **named** `modules/named/commands/override-in-address.sh:29-33` — Despite help text 'Override the IN A ... in named DNS server zone file', the command only regenerates certificates and haproxy, never namedcfg/restart_named, so the DNS zone change silently waits until the next full regenerate.
- **named** `modules/named/libs/install.sh:50` — Dormant install_dyndns uses $CDN which is never defined in any bash scope (tests /etc/srvctl/cert//.key), chowns the never-created /var/dyndns/srvctl.private (line 77), and uses dnssec-keygen -a HMAC-MD5 which modern BIND removed.
- **named** `modules/named/named.js:252-253` — For a use_gsuite domain missing dkim-google-domainkey the DMARC record is silently omitted (err only prints to stdout), weakening mail policy without failing the run.
- **nfs** `modules/nfs/hooks/update-install-host.sh:11` — nfs_mount runs before rpcbind/nfs-server are enabled (lines 13-14), so on first bootstrap the showmount probes fail and print 'Could not mount' errors; mounts only appear on a later regenerate.
- **nfs** `modules/nfs/hooks/diagnose.sh:9` — showmount -e runs without a timeout (unlike nfslib.sh:24), so a host that answers ping but filters RPC stalls sc diagnose for the full RPC timeout per host.
- **nfs** `modules/nfs/hooks/diagnose.sh:7` — diagnose pings $host by DNS name while real NFS traffic uses the VPN IP 10.15.$hs.1 (nfslib.sh:21,27), so the diagnostic tests the wrong network path and can pass or fail misleadingly.
- **nfs** `modules/nfs/libs/nfslib.sh:5` — nfs_generate_exports overwrites /etc/exports wholesale with cat >, silently destroying any admin-maintained export entries on the host.
- **nfs** `modules/nfs/libs/nfslib.sh:17` — Loop variable host is not declared local (only hs is); since hooks are sourced into the main shell, nfs_mount clobbers the global host variable used by other sourced hooks/libs in the same run.
- **ntp** `modules/ntp/hooks/update-install-host.sh:5` — chronyd (Fedora default, mutually conflicting time daemon) is never disabled, leaving two enabled Conflicts= units after reboot with transaction-order-dependent outcome — the host can silently end up back on chronyd or with no time daemon running.
- **ntp** `modules/ntp/hooks/update-install-host.sh:3` — No idempotence guard (unlike named/libs/install.sh:10 style checks): every update-install re-runs `dnf install ntpsec`, which combined with the medium bug aborts the whole update-install on an offline machine even when ntpd is already installed and running.
- **odoo** `modules/odoo/commands/install-odoo.sh:191` — Postgres block is guarded only by pg_hba.conf existence, so if initdb succeeds but role/template setup fails (or postgres was preinstalled) re-runs skip the block forever and the odoo DB role is never created; same fragile-guard pattern at 212, 287.
- **odoo** `modules/odoo/commands/install-odoo.sh:117-121` — Redundant $USER!=root re-check after root_only can spuriously exit 1 in cron/systemd contexts where USER is unset despite UID 0, and its exit code 1 deviates from srvctl's auth-failure code 44.
- **odoo** `modules/odoo/commands/install-odoo.sh:128` — Because the file is sourced, `trap cleanup 0`, the line-371 ERR trap, the errexit option, and the hr_HR.UTF-8 locale exports all leak into the persistent srvctl shell after the command finishes (cleanup itself is dead code — tempfiles is never populated).
- **odoo** `modules/odoo/commands/install-odoo.sh:291-292` — `$1` inside the su venv-setup string expands to run_command's always-empty first positional (leftover dry-run mechanism from the standalone installer); dead now but breaks venv creation if dispatch ever passes arguments.
- **opendkim** `modules/opendkim/hooks/update-install-host.sh:44` — regenerate_opendkim is commented out but add_service starts opendkim before /var/opendkim/{KeyTable,SigningTable,TrustedHosts} exist, so on a fresh host the service fails to start and mail goes unsigned until the first sc regenerate.
- **opendkim** `modules/opendkim/opendkim.js:90` — Domain name from containers.json is interpolated unescaped into a root execSync shell command (opendkim-genkey), giving root shell injection if the datastore is corrupted or attacker-writable.
- **opendkim** `modules/opendkim/hooks/version.sh:3` — Dead hook: nothing in the tree ever runs 'run_hook version', so opendkim is never reported by sc version despite the file existing.
- **opendkim** `modules/opendkim/libs/opendkimlib.sh:19` — Unquoted glob chmod 640 /var/opendkim/*/* passes the literal pattern to chmod when no domain dirs exist, printing an error on fresh clusters (harmless noise, execution continues).
- **openvpn** `modules/openvpn/hooks/adjust-service.sh:31` — Conf globs iterate without nullglob (also lines 15 and 41), so with an empty conf directory the loop calls service_action on the literal pattern (e.g. `openvpn-server@*`), producing systemctl failures for enable/restart ops.
- **openvpn** `modules/openvpn/hooks/update-install-host.sh:24` — `run chmod 600 /etc/openvpn/*.key.pem` with an unmatched glob (e.g. after a failed cert grab) chmods the literal string and errors, obscuring the real failure cause.
- **password** `modules/password/libs/bashlib.sh:9` — Failure message 'SSH-ERROR cfg ...' is copy-pasted from an SSH lib, so a missing/broken node aborts srvctl with a misleading SSH error.
- **password** `modules/password/libs/get-password.sh:13` — adl/aal/arl/bbl/bcl are assigned without local, leaking five globals into every sourcing srvctl shell and risking clobbering same-named caller variables.
- **perdition** `modules/perdition/hooks/firewalld.sh:7` — The ${container:0:5}==mail. branch is dead ($container is unset in the only host-context trigger path and the module is inactive inside VEs), and even if it fired, opening imap is pointless since imap4.service binds 127.0.0.1 only.
- **perdition** `modules/perdition/libs/install_perdition.sh:24` — Seeds /etc/perdition/popmap.re although perdition.conf points map_library_opt at /var/perdition/popmap.re, leaving a never-read decoy file that misleads routing debugging; also rm -fr of RPM-owned unit files at line 35 is undone by any package update.
- **perdition** `modules/perdition/perdition.js:79` — Domain names are interpolated into the popmap regex unescaped (dots match any char, so user@exampleXcom also matches) and every container — including pure web sites — is mapped to a possibly nonexistent mail.<domain> backend, with duplicate lines when both example.com and mail.example.com containers exist.
- **postfix** `modules/postfix/hooks/regenerate.sh:5` — Unconditional full 'systemctl restart postfix.service' (not reload) on every container/user add drops in-flight SMTP connections cluster-wide even when only the relaydomains map changed.
- **postfix** `modules/postfix/hooks/version.sh:3` — Dead hook — nothing in the tree calls run_hook version, and 'sc version' hardcodes the identical msg_version_installed postfix call (modules/srvctl/commands/version.sh:11).
- **postfix** `modules/postfix/libs/postfixlib.sh:67` — write_postfix_main has no callers and its stated in-container use is unreachable because the module condition is always false inside nspawn containers (modules/containers/module-condition.sh:12-19) — dead code.
- **postfix** `modules/postfix/hooks/update-install-host.sh:29` — make_aliases_db '' overwrites host /etc/aliases with the static template on every 'sc update-install', silently destroying any locally added alias such as a root: mail forward.
- **postfix** `modules/postfix/conf/hs-main.cf:71` — smtpd_recipient_restrictions ends at permit_sasl_authenticated with reject_unauth_destination commented out, so anti-relay depends only on Postfix's implicit smtpd_relay_restrictions default while mynetworks (line 20) whitelists all of 10.0.0.0/8 and 192.168.0.0/16 — any device on a shared RFC1918 segment can relay through the host.
- **saslauthd** `modules/saslauthd/commands/testsaslauthd.sh:11` — root_only (exit 44 for non-root) precedes sudomize, making the sudo self-elevation dead code and inconsistent with fix-saslauthd which elevates non-root callers.
- **saslauthd** `modules/saslauthd/commands/testsaslauthd.sh:16` — password variable is never initialized so an exported caller env var silently passes the not-found check, and unquoted $domain/$user in the [ -f ... ] tests break on args with spaces or globs.
- **saslauthd** `modules/saslauthd/hooks/version.sh:3` — Dead hook: no run_hook version call site exists anywhere in the tree, so this file never executes.
- **saslauthd** `modules/saslauthd/hooks/regenerate.sh:3` — Unconditionally restarts saslauthd on every regenerate (11 call sites incl. every add-ve/add-user), causing a momentary SMTP AUTH outage for all mail users on each container operation despite no domain-dependent config in saslauthd.
- **srvctl** `modules/srvctl/libs/authlib.sh:54` — In sudomize's failure branch `exit $?` runs after `debug` (which returns 0), so a failed sudo re-exec exits 0, masking the real error code.
- **srvctl** `modules/srvctl/libs/authlib.sh:6` — root_only echoes leftover debug 'SC_UID0 true' to stdout inside every root-only command, polluting captured/parsed output.
- **srvctl** `modules/srvctl/libs/moduleslib.sh:5` — reset_modules misses /root/.srvctl/modules.conf (the cache root actually uses per commonlib.sh:416-419) and is never called anywhere — dead and incomplete.
- **srvctl** `modules/srvctl/commands/customize.sh:31` — Copy-from-srvctl branch reads $SC_INSTALL_DIR/commands/$arg.sh, a directory that does not exist in v3 — dead v2-era path.
- **srvctl** `modules/srvctl/commands/update-install.sh:54` — The interactive mcedit-hostname else-branch is unreachable because lines 22-28 already exited when ARG is empty.
- **ssh** `modules/ssh/ssh.js:249-251` — Unguarded hosts[i].host_ip/hostnet emit literal 'undefined ssh-rsa ...' and '10.15.undefined.undefined' lines into both known_hosts files when fields are missing.
- **ssh** `modules/ssh/ssh.js:141-144` — user_keys iterates the cluster-wide containers.json, creating orphan share/containers/<C> dirs for containers on other hosts, and non-recursive mkdirSync crashes the run if the parent dir is missing.
- **ssh** `modules/ssh/ssh.js:170` — Success message names srvctl-hosts.conf while the file written is srvctl-chosts.conf, misleading operators.
- **ssh** `modules/ssh/libs/bashlib.sh:49-53` — chown/chmod run on share/common/authorized_keys and ssh/known_hosts even when the files were never created, printing errors on pristine installs.
- **sshpiperd** `modules/sshpiperd/libs/sshpiperlib.sh:10 (with hooks/regenerate.sh:3)` — The bindfs mount of /var/sshpiper is created only at hook time (no fstab/.mount unit), so after a host reboot the daemon runs against an empty dir and all proxy auth fails for up to ~1h until the hourly regenerate cron remounts it; a bindfs failure also aborts the whole regenerate run via run_hook's exif.
- **sshpiperd** `modules/sshpiperd/build.sh:7-10` — Inverted existence test warns 'patch file missing' when workingdir.go exists and continues silently when missing, after which line 26's redirection truncates the upstream Go file to empty when run from the wrong cwd.
- **sshpiperd** `modules/sshpiperd/build.sh:31-32` — Uses the srvctl 'run' helper in a standalone script where it is undefined ('run: command not found'), so the documented rebuild path cannot work at all; the GOPATH go-get workflow (lines 19-22) is also dead under module-mode Go and the @SRVCTL_INSTALL_DIR sed (line 29) is a no-op.
- **sshpiperd** `modules/sshpiperd/workingdir.go:176` — Client-supplied username is string-concatenated into a bash -c command; safe only because checkUsername runs first at line 165 — starting the daemon with --allow_bad_username would turn this into remote shell injection as user sshpiper.
- **static** `modules/static/server.js:21-22` — The 'static.' prefix strip reads from the original req.headers.host instead of the www-stripped host, so www.static.example.com resolves to 'tic.example.com' and serves the wrong docroot/404 (a Host with :port is likewise never stripped).
- **static** `modules/static/hooks/init.sh:3` — if $SC_USE_GLUSTER with an unset variable expands to an empty command list (status 0 = true), so a stale modules.conf missing the var would call the undefined gluster_mount_data/gluster_configure (exit 127, masked by return 0); same pattern at hooks/update-install-host.sh:3.
- **static** `modules/static/server.js:7` — Runtime hard-codes require('/usr/lib/node_modules/...') while the installer relies on unpinned 'npm install -g' (hooks/update-install-host.sh:33-34) landing exactly there; a different npm prefix or future package major bump turns startup into a Restart=always crash loop.
- **usersonhost** `modules/usersonhost/main.js:179` — Reseller srvctl-key symlink is named `srvctl_reseller_id_ecdsa.pub` here but `reseller_srvctl_id_ecdsa.pub` in ssh/libs/userlib.sh:61; the two provisioning paths disagree on the filename (latent, no current consumer).
- **usersonhost** `modules/usersonhost/main.js:113` — `fs.chmodSync(dir, 0600)` sets the datastore user dir to mode 0600 (no traverse bit) each run, so any non-root reader (e.g. an AuthorizedKeysCommand user) cannot reach the *.pub files inside.
- **usersonhost** `modules/usersonhost/commands/add-publickey.sh:40` — After removing an empty $pub at line 37, the unconditional `cat "$pub"` at line 40 emits a stray 'No such file' error on the empty-input path.
- **usersonve** `modules/usersonve/commands/add-zerotier.sh:34` — USER WIP file (present in HEAD too): bare 'exit' after err 'No network specified' returns status 0, so this error path reports success.
- **usersonve** `modules/usersonve/commands/add-user.sh:41` — Plaintext password persisted in $home/.password created with root's umask (typically world-readable 0644), and echo -e mangles passwords containing backslash sequences; re-running add-user silently resets an existing user's password to stale .password content.
- **usersonve** `modules/usersonve/commands/add-user.sh:1` — Missing '[[ $SRVCTL ]]' guard (all sibling commands have one), so direct bash execution stumbles through undefined helpers argument/err with no set -e.
- **usersonve** `modules/usersonve/commands/vnc-desktop.sh:114` — 'echo "" > /home/x/.xbindkeysrc' immediately truncates the xbindkeys defaults generated on line 112, making that generation dead work.
- **usersonve** `modules/usersonve/commands/vnc-desktop.sh:58` — The gnome branch prints 'unimplemented' then bare exit (status 0), so requesting the gnome desktop silently succeeds while installing nothing.
- **usersonve** `modules/usersonve/commands/install-crossover.sh:29` — ln -s without -f (also lines 35-36) fails on re-runs, breaking idempotency and emitting error warnings on every repeat install.
- **ve** `modules/ve/commands/status.sh:3` — Command has only the `## @en` hint and no mandatory `## &en` help lines, so `sc help status` renders an empty help body, violating the documented help contract.
- **ve** `modules/ve/hooks/pre-init.sh:4` — `readonly SC_VIRT=$(systemd-detect-virt -c)` is dead code in the main shell (no consumer anywhere; all other SC_VIRT uses are in condition subshells), forks a process on every invocation in a VE, masks the command's exit status, and its readonly mark makes any future SC_VIRT assignment in the same shell fatal.
- **ve** `modules/ve/commands/status.sh:1` — status.sh and hooks/pre-init.sh lack the conventional `[[ $SRVCTL ]] || exit 10` guard, so executing them directly runs w/du outside srvctl (shared smell with several other modules' hooks).
- **vncproxy** `modules/vncproxy/start.sh:24` — forward_key is PRIMARY KEY and 8-char hash collisions between (container,user) pairs make the second INSERT fail with the sqlite3 error unchecked, so that user silently loses VNC access on every DB rebuild.
- **vncproxy** `modules/datastore/main.js:157` — Cross-module: duplicated 'if (CMD === ADD)' blocks make 'add container C vncuser U' fire two concurrent async writes of different content to containers.json; if the stale (pre-push) write lands last the just-added vncuser is lost.
- **vncproxy** `modules/vncproxy/commands/add-vnc-user.sh:9` — Command has no root_only marker or authorize call, so non-root users see it and a non-root run half-executes (datastore write may succeed, systemctl restart fails), leaving records/DB out of sync.
- **wordpress** `modules/wordpress/commands/install-wordpress.sh:1` — Missing the conventional `[[ $SRVCTL ]] || exit 10` guard: running the file directly with bash skips undefined helpers but still executes the raw cat/chown lines, clobbering /etc/httpd/conf.d files and wp-config.php with empty DB credentials.
- **wordpress** `modules/wordpress/commands/install-wordpress.sh:56` — unzip without -o with stdout redirected to a log will prompt invisibly for overwrite if /root/wordpress remains from an aborted run (appears hung on a tty, EOF-errors non-interactively).
- **wordpress** `modules/wordpress/commands/install-wordpress.sh:51` — Correct DB naming for hyphenated hostnames depends on add_mariadb_db reassigning the caller's global db_name to the sanitized value (mariadblib.sh:97-103); a rewrite that breaks this hidden coupling makes wp-config point at a nonexistent database.
- **wordpress** `modules/wordpress/scripts/restore-wordpress-password.sh:16` — Dead HASH computation (md5 of password including echo's trailing newline) and plaintext password echoed to stdout (:15,77,81) — misleading code and credential leakage into terminal/logs.
