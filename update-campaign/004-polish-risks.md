# 004 — Polish-risk register (from discovery, session 1)

Condensed "must not change" notes per unit; full versions in each fact sheet.


## backup
Function names are the external API (out-of-repo scripts / exec-function): local_backup, server_backup, remote_backup, display_backup_geometry, local_container_7z_backup. Defaults SC_BACKUP_PATH=/backup (sclib.sh:3) and BACKUP_PATH=/backup (7zlib.sh:11). Target layout $SC_BACKUP_PATH/<hostname>/$(dirname dir)/ and 7z layout $BACKUP_PATH/$HOSTNAME/$C/{cert,srv,home,root,etc,var,var-lib-mysql}.7z + filelist/packagelist/creation-date. Exact backup.log line formats (sclib.sh:10,38,90,92,134,188,190,232,288,290) including the misspelled `local-backup-failure` label in all three functions. User-visible strings incl. typo "$i dont exists" (sclib.sh:40), "There is a process already running for this backup task.", "Calculating ...", "OK backup done ...". rsync flag sets: `--progress --delete-after -a` / `-avze ssh` / `-avz -e "ssh -A $proxy ssh"` with root@host source for remote_backup, each preceded by an identical `nur` banner. ssh probe options `-n -o BatchMode=yes` and `-n -o ConnectTimeout=1`. 7z switch string `7z u -uq0` (update semantics). Error paths return/continue rather than exit. module-condition.sh must emit exactly "true"; 7zlib.sh's top-level `return` requires the file stay sourced, not executed.

## backupdb
Keep function name backupdb (only entry point: sc exec-function backupdb; documented in documentation.md:335,1361). Keep exact ntc lines "mariadb @ $HOSTNAME" (backupdblib.sh:7) and "mongodb @ $HOSTNAME" (:15). Keep dump path/layout /root/mongodb-dump/$(date +%Y_%m_%d__%H_%M_%S) (:28) and wipe-before-dump one-generation semantics (:27). Keep detection predicates: -d /var/lib/mysql && -f /usr/bin/mysql (:5), -d /var/lib/mongodb (:13). Keep vendored binary path $SC_INSTALL_DIR/modules/backupdb/bin/mongodump (:30). module-condition.sh must output exactly "true" (parsed by commonlib.sh:436-442, cached in modules.conf). mariadblib is sourced by absolute path regardless of SC_USE_MARIADB (:9), and backup_mariadb exits the whole process via exif on mysqldump failure (mariadblib.sh:75), skipping the mongo branch — preserve that ordering/exit behavior.

## branding
Cross-module function API must keep names/args: setup_index_html(name,dir) called from containers addcontainerlib.sh:63 and static regenerate.sh:10; setup_varwwwhtml_error(code,text) from hooks/update-install-host.sh:5-15. Output paths /var/www/html/$code.html and $code.http for exactly codes 400,403,404,408,414,500,501,502,503,504 — haproxy.js:230-243 fs.existsSync-checks the .http paths, and .http files must stay raw valid HTTP/1.1 responses (status line + Cache-Control/Connection/Content-Type/Retry-After headers + blank line, update-install-lib.sh:48-53) since haproxy serves the bytes verbatim. Byte-identical page output includes the per-code English texts (with the "apgain" typo), the 31-language ERROR! blob, and the stray literal quotes in index.html (brandinglib.sh:14-34). setup_index_html must keep silently no-oping when the dir is missing (brandinglib.sh:11) and copying favicon.ico into the docroot (line 36). Env contract: after post-init, SC_COMPANY and SC_COMPANY_DOMAIN are readonly+exported, defaulting to $HOSTNAME; SC_RESELLER_USER defaults to root but stays UNexported (exporting it would change what datastore main.js sees). Preserve msg string "Writing Error files in /var/www/html" and activation identical to the containers module condition (module-condition.sh:4).

## ca
Defaults SC_ROOTCA_DIR=/etc/srvctl/CA, SC_ROOTCA_HOST=$HOSTNAME, SC_ROOTCA_SUBJ=/C=HU/ST=Hungary/L=Budapest/O=SRVCTL-CA (pre-init.sh:4-8) and their readonly promotion in post-init.sh. On-disk layout other modules rsync by exact path: ca/$net.{key,crt}.pem, ca/$net.srl (seed 02), $net/{server|client}-$name.{key,crt}.pem, usernet/client-$user.p12, tmp/$file.csr.pem. Crypto params: RSA 4096, CA 3652 days, leaf 1095 days, -checkend 86400 renewal, CA CN $SC_COMPANY-$net-ca, leaf CN $name, empty p12 passphrase, key chmod 600. Function names/signatures are cross-module API (certificates, gluster, openvpn; also called remotely via srvctl exec-function). Silent no-op off the CA host (create_ca_certificate/root_CA_init return without error). Exact msg/err strings including the 'No CA server definded in configs' typo (netlib.sh:18). extfile path modules/certificates/openssl-server-ext.cnf with -extensions server. ca_sync rsync destination /etc/srvctl (parent) and the fact that calib.sh:148 passes '$_ext' as one quoted (possibly empty) arg that only works because run expands $* unquoted — a rewrite that quotes run's args properly breaks client cert issuance.

## certificates
check_pem must keep returning 0 unconditionally AND deleting pems failing `openssl x509 -checkend 604800` (haproxy proxylib.sh:8-16 depends on delete-then-recheck). Exit codes: 46 (cert without key, domaincertlib.sh:109), guard exit 4 (update-install-host.sh:5). File layout from create_selfsigned_domain_certificate: $domain.key/.key.org/.csr/.crt, $domain.pem = key FIRST then crt, cert.pem copy, config.txt/extfile.txt; RSA 2048, 3650 days, SAN domain + *.domain, keyUsage set that avoids Chrome ERR_SSL_KEY_USAGE_INCOMPATIBLE. install_service_hostcertificate search order (SC_COMPANY_DOMAIN → ${HOSTNAME:3} → $HOSTNAME → first subdir → self-signed) and outputs crt.pem(+dhparam)/key.pem/ca-bundle.pem chmod 400, dhparam cached at $src/dhparam. check_wildcard_pem echoes bare domain or literal "false", handles both "CN = *." (offset 15) and "CN=*." (offset 13) subjects. Wildcard fan-out target name $SC_DATASTORE_DIR/cert/$c.pem and match rules *.domain / domain. Hook filename regenerate_certificates is API (invoked by name from haproxy and named). openssl-server-ext.cnf path is hardcoded by ca module (calib.sh:97) with section name "server" and nsCertType=server expected by openvpn. Activation must remain identical to containers module condition. msg/ntc strings listed in fact sheet (e.g. "Imported $dom certificate for $path", "Remove $cert_path manually to create a new certificate.").

## codepad
Exit code 13 for both add-codepad name validations and their exact err strings (commands/add-codepad.sh:11-21); help text lines @@@/@ en/&en (add-codepad.sh:3-6); the *-devel name rule which haproxy.js:388 relies on to gate ports 9000/9001/24678; firewalld service names persisted as XML (host: http9000, https9001 per hooks/firewalld.sh:4-5; template: http, https, http8080, https8443, https9200, https9000, https9001 per hooks/mkrootfs_fedora.sh:11-23 — note 9000 is named differently host vs template); uid math C_uid+804 (libs/init_codepad_project.sh:18) synced with useradd -u 804 codepad (containers/libs/mkrootfs_fedora.sh:65-66); datastore type recorded as 'fedora' for codepad containers (containers/libs/addcontainerlib.sh:40-44); filesystem contract consumed by the out-of-repo codepad app: /var/codepad, /var/codepad/users symlink to /var/srvctl3/share/containers/$C/users, credential filenames .hash/.password/.ip, /srv/codepad-project, @boilerplate/@codepad-modules/boilerplate symlinks, /var/git; codepad.service unit body (node /var/codepad/server.js, User/Group codepad, Restart=always, NODE_ENV=production; mkrootfs...:68-86); server.js stub semantics (global ß, ß.theme="cobalt", require("./boilerplate"); mkrootfs...:90-114); .profile (cd /srv/codepad-project + mc) and .gitconfig push.default=simple; codepad pubkey appended to container root authorized_keys (init_codepad_project.sh:15-16); access.js success message 'codepad: user and users access keys configured' and exit 0 / DATA-ERROR 111 convention; exif abort message 'CODEPAD-ACCESS-ERROR cfg $* ($?)' (libs/configure_codepad_access.sh:11); SC_USE_CODEPAD env consumed by haproxy.js:65 and vncproxy.js:65; recreate-ve expects /srv/$C/rootfs/srv/codepad-project/boilerplate/install.sh (containers/commands/recreate-ve.sh:122-125).

## containers
Exit codes: 4 (SRVCTL guard), 10/11/12 (add_ve), 17 (start failure), 22 (bad username), 35, 44 (hs_only/ve_only), 66 (missing base rootfs), 15/11 in execstartpre (systemd aborts start on them), execstartpost must exit 0, host-conf.js 113, 'get cluster container_list || exit 15'. Literal marker strings hs_only/ve_only/reseller_only/root_only must stay within the first 20 lines of command files (commonlib.sh:221 greps them). Unit name srvctl-nspawn@%i at /etc/systemd/system with SuccessExitStatus=133, Type=notify, ExecStartPre/Post/StopPost calling $SC_INSTALL_DIR/modules/containers/execstart*.sh %i; machines.target.wants symlink path. Paths: /srv/$C/{rootfs,$C.nspawn,local.nspawn override,hosts,network/,ethernet.sh,firewall_cmd.sh,*.binds,binds/*.binds,cert/}, /var/srvctl3/share/containers/$C/config, /var/srvctl3/datastore/cert/$C.pem, /var/srvctl3/rootfs/<type>, backup layout $SC_BACKUP_PATH/srvctl-containers/$C/$NOW, /etc/cron.hourly/srvctl-regenerate.sh with literal ARG '#cron.hourly' matched in hooks/regenerate.sh:13. Defaults: SC_HOSTNET=250, SC_CLUSTERNAME=test_cluster, SC_ROOTFS_DIR=/var/srvctl3/rootfs, SC_MOUNTS_DIR=/var/srvctl3/mounts, SC_BACKUP_PATH=/backup, type fedora, quota 250000000 KB; exports SC_HOSTNET/SC_CLUSTERNAME/SC_ROOTCA_HOST for node. Hook names consumed by other modules: add-ve, add_ve_$T, add_ve_create_nspawn_container, mkrootfs_fedora, mkrootfs_debian, regenerate, regenerate_rootfs; add_ve maps type codepad->fedora; container restart must stay stop-then-start (systemd#2809). Contracts: /srv perms 0750 drwxr-x---, base-image uids srv=801 git=802 node=803 codepad=804, sc/srvctl symlinks in images, resolved.conf DNS=8.8.8.8 in new VEs, /etc/hosts regenerated from 'get cluster etc_hosts', inotify sysctl bump each regenerate, status table header 'REACTION VE IP TYPE DISK USER RESELLER INFO', messages '$C destroyed.', 'regenerate done', '$C READY', '[execstartpre]/[execstartpost]' journal prefixes, GUI '## spec //containers×...' lines and ## @@@/## @en help format, sc <VE> shell|status|reboot|poweroff|kill|show|login shorthand.

## core
Preserve exactly: marker strings '## @@@'/'## @en'/'## &en'/'## &&&' with 7-char offset stripping and head-window semantics (commonlib.sh:7-13,229,242,245,326) plus root_only/hs_only/reseller_only markers in first 20 lines; hint layout (3 spaces, %-40s cmd, %-48s hint, green) because generate_completion captures hint_commands output into /var/local/srvctl/completion/$USER.{hints,commands,arguments,VE} parsed by modules/srvctl/completion.sh; msg/ntc/err '[ shorthost ]' colored prefixes, err to stderr + $SC_LOG; exit_0 trailer '## srvctl-<version>' suppressed for exec-function (lablib.js depends on it) and DEBUG+TTY; SRVCTL='srvctl-<version>' guard token ([[ $SRVCTL ]] || exit 10 in all modules); exit codes (10 guard, exif propagates exact code, final exit 1 with 'Invalid command. $CMD'/'No-command.', help/complicate/test-modules exit 0); ?/+/-/! aliases on $1 and $2 and CMD lowercasing (ARG not lowercased); dispatch precedence exec-function -> datastore verbs (new/get/put/out/cfg/del/add) -> /root/srvctl-includes -> module commands in SC_MODULES order (root custom modules first) -> $SC_HOME/srvctl-includes (non-root) -> command.sh defaults (containers sources srvctl/command.sh), commands sourced in the CLI shell; hook order pre-init-$CMD before pre-init, then help breakout, load_libs, init, post-init, post-init-$CMD; modules.conf line format 'export SC_USE_<NAME>=true|false' at $SC_HOME/.srvctl/modules.conf (+legacy /var/local path sourced first), regenerated on missing/update-install/test-modules, vars readonly-exported; env exports SC_INSTALL_DIR/SC_HOME/SC_USER/SC_UID0/SRVCTL/NOW consumed by node apps; log formats and paths ~/.srvctl/srvctl.log, /var/log/srvctl-root.log, NOW=%Y.%m.%d-%H:%M:%S; /bin/sc, /bin/srvctl, /etc/bash_completion.d/srvctl-completion symlink targets; /etc/srvctl/*.conf bash sourcing and /etc/srvctl/data materialization with chmod 644 host.conf/hosts.json; set_permissions mode map; user-visible strings ('Usage: srvctl command [argument]', 'Pardon? ...', 'tested module: ...').

## datastore
Exit-code contract must be preserved: get returns 0=value(printed)/100=missing-optional/other=error; MAIN-ERROR: exit 110, LIB-ERROR: exit 112, default exitCode 99. `out` format VAR='value' with -→_ and JSON.stringify'd objects; `out container` emits C=, `out host` emits SC_HOSTNAME=/SC_<KEY_UPPER>=; `out container X json` = 4-space pretty JSON. IP scheme 10.<hostnet>.<user_id>.<c> (c from 2, >250 fatal); container_uid=65536*(user_id*255+c); interface naming 192→a_b_c/172→a+b+c/else b-c-d; bridge default 10.<b>.<c>.x. datastore-server on port 1030 serving exactly /.well-known/srvctl/datastore/containers.json and /.well-known/pki-validation/<file>; haproxy hardcodes 127.0.0.1:1030. datastore_push appends to .git.log and commits msg "$SC_USER@$HOSTNAME <args>"; .gitignore=.git.log+.gitignore. systemd unit datastore-server.service (User=root, Restart=always, RestartSec=3, WantedBy=multi-user.target). Default dirs /var/srvctl3/gluster/srvctl-data (RO) and /var/srvctl3/datastore (RW), SC_DATASTORE_RO_USE default true. Defaults container_quota 250000000, http 80, https 443, resolv appends 8.8.8.8/8.8.4.4. default-users.json seed (root + a–x) must stay exact.

## default
Port 1282 (hardcoded consumer at modules/haproxy/haproxy.js:513); unit name/path /etc/systemd/system/default-server.service and its exact contents (Description=srvctl default page server, ExecStart=/bin/node $SC_INSTALL_DIR/modules/default/server.js, User/Group=nobody, Restart=always); response semantics always 404 + Content-Type: text/html with body from /var/www/html/404.html readable by nobody; journal strings "srvctl default-server started" and per-request "host url user-agent x-forwarded-for" log line; hook stays at update-install-host and never fails the update-install run; activation mirrors SC_USE_CONTAINERS; branding module's hook must keep running before default's (alphabetical SC_MODULES order) so 404.html exists at first service start.

## dns
Datastore schema written to $SC_DATASTORE_DIR/containers.json must stay identical: containers[name].dns[domain] = {A:[], AAAA:[], MX:[{exchange,priority}], NS:[], timestamp:{time:<epoch-seconds>, state:"OK"|"UNKNOWN"|<node dns err.code like ENOTFOUND/ETIMEOUT/ESERVFAIL/ENODATA>}}, pretty-printed JSON.stringify(...,null,2) — letsencrypt.js:283-299 consumes this shape. Preserve the 3600s skip window applying only to states ENOTFOUND/ETIMEOUT/ESERVFAIL (dns-scan.js:105-109), the scanned domain set from datastore.container_domains (name if dotted + www. + aliases/altnames + www. variants), and dns-scan.js exiting 0 on normal runs (hook failure aborts srvctl via exif in run_hook, commonlib.sh:104). Preserve output strings: msg "DNS scan" (bashlib.sh:4), "No container for <domain>" (dns-scan.js:85), "Skipping DNS scan on <STATE> <name> <domain>" (dns-scan.js:106-108), ntc "DNS Scan A record <code> <hostname>" (dns-scan.js:117), "Set 8.8.8.8 as DNS server globally." (update-install-host.sh:3). Hook lifecycle points (scan on regenerate; resolved.conf write + systemd-resolved restart on update-install-host) and the global bash function name dns_scan are observable behavior on production hosts.

## firewalld
Function names/signatures firewalld_add_service(name, proto, port, hint) and firewalld_offline_add_service(name, proto, port) are a cross-module ABI (10+ call sites plus generated code at modules/datastore/lib.js:665 using port-<n> service names). Service names http8080/https8443/https9200 and the path pattern /etc/firewalld/services/$name.xml must stay byte-identical for idempotency against XML files already deployed on production hosts. Preserve: XML heredoc shape with description strings "(srvctl added for $hint)" and "(added by srvctl)"; hint defaulting to $SC_USER; default-zone targeting via --get-default-zone (not hardcoded public); write-XML -> reload -> --permanent --add-service -> reload ordering; ntc messages "firewalld: $name is enabled already." / "firewalld-offline: $name is enabled already."; masquerade enabled permanent+runtime on the host default zone (container NAT depends on it); the rootfs_base variable contract and rootfs_name==fedora gate for the offline path; the module-condition truth table (true in containers, true when hostname in hosts.json, true for update-install with ARG, false on localhost.localdomain); and sourced-hook exit-code semantics (last command's status becomes fatal via run_hook's exif).

## ftp
Hook filenames hooks/update-install-host.sh and hooks/firewalld.sh are looked up literally as $dir/hooks/$hook.sh (commonlib.sh:97). Module dir name `ftp` yields the cached SC_USE_FTP variable persisted in modules.conf on production hosts (commonlib.sh:431,448). firewalld service name must stay exactly `ftp` (idempotency via `firewall-cmd --permanent --query-service=ftp`, firewalldlib.sh:21, and stock /usr/lib/firewalld/services/ftp.xml check, firewalldlib.sh:27). Package name `vsftpd` via idempotent `dnf -y -q install` (fedoralib.sh:10). module-condition.sh must keep delegating to containers/module-condition.sh and emit exactly `true` to enable (commonlib.sh:437), including the `update-install + ARG` bootstrap path. Both hooks' exit statuses currently propagate to run_hook's exif; swallowing failures would change update-install abort behavior. No module-local output strings exist (rerun messages come from firewalldlib.sh:23,29).

## gluster
SC_USE_GLUSTER is derived from the module directory name and is tested by datastore/static hooks; module-condition.sh must keep emitting literal false on all paths at baseline. Cross-module function contracts must survive: gluster_configure(datadir,mountdir) and gluster_mount_data(datadir,mountdir) names/signatures, and gluster_mount_data's exit code (0 iff fuse-mounted, glusterlib.sh:202/205) which drives SC_DATASTORE_RO_USE — including today's buggy 0-returns at glusterlib.sh:141-159 unless fixed in sync with datastore. Preserve path schemes: brick /glu/<datadir>/brick, RO bind mount /var/srvctl3/gluster/<datadir> (consumed by datastore pre-init and ssh/sshd_authorization.sh), /etc/ssl/gluster-{ca.crt,server.key,server.crt}.pem, symlinks /etc/ssl/glusterfs.{ca,pem,key}, /var/lib/glusterd/secure-access, /etc/srvctl/CA/{ca/gluster.crt.pem,gluster/server-<host>.*}, /etc/ssl/dhparam.pem, mount log /var/log/gluster-<datadir>-mount-$NOW.log. Preserve gluster invocations (replica <hostcount> force, client.ssl/server.ssl on, start force), firewalld service name glusterfs, CA net name 'gluster', and hook filenames diagnose.sh/update-install-host.sh. Two quoting landmines: run rsync "$options" only works because run expands $* unquoted (lablib.sh:105) — proper quoting breaks rsync; and the load-bearing grep patterns on mount/gluster output (glusterlib.sh:162,172,184,198) must stay byte-identical.

## gui
Preserve: /var/local/srvctl/commands.spec path and its ×(U+00D7)-delimited 4-field record format path×command×hint×syntax (spec.sh:17) plus the '## spec //cat×cmd×hint×args' passthrough convention (spec.sh:59; producers containers/command.sh:9-13, srvctl/command.sh:18-22) whose //cat prefix drives server.js:81 category parsing; exclusion of files with 'root_only' or '## interactive' in first 10 lines (spec.sh:5-6); fixed 7-char strips tied to '## @en '/'## @@@ ' prefixes and .sh suffix strip (spec.sh:13-15); function name make_commands_spec and caller contract `if $SC_USE_GUI` (modules/srvctl/commands/update-install.sh:85-88) plus msg string 'Make user commands spec for srvctl-gui.' (spec.sh:22); activation semantics false-only-in-nspawn/lxc (module-condition.sh:6-12); daemon contract on legacy hosts: HTTPS port 250, client-cert auth with /etc/srvctl-gui/{crt,key}.pem and CA /etc/srvctl/CA/ca/usernet.crt.pem, username=cert CN (server.js:8,28-34,175-177); user key path $SC_DATASTORE_DIR/users/<CN>/srvctl_id_ecdsa (server.js:180, shared with ssh module and usersonhost); datastore reads containers/users/hosts.json and owner filter containers[i].user===CN (server.js:59-61,108-110); routes /ssh/:user and static mounts from /usr/lib/node_modules (server.js:7,46-55); socket.io event names main/lock/terminal/command/get-main/output/input/resize; exec semantics: command+' 2>&1' over ssh, localhost→HOSTNAME mapping, container→root@container, pty ssh with -o PreferredAuthentications=publickey -i keyfile 80x30 xterm-256color (server.js:127-139,194-199); unit name srvctl-gui.service and firewalld service srvctl-gui tcp 250 (hook:13,50); srvctl-gui/hterm_all.js is vendored — keep byte-identical.

## haproxy
Generated /etc/haproxy/haproxy.cfg structure and names: frontends http/https/port<N>, backends http:<ve>, https:<ve>, port<N>:<ve>, default; ACL names .well-known-acl/letsencrypt-acl/thunderbird-acl/srvctl3data-acl/pki-validations-acl with targets 127.0.0.1:1028/1029/1030 and default localhost:1282; redirect exception suffix " !.well-known-acl". Semantics: absent http-redirect key means default 301 http->https, 'none' means serve directly, keyword flips protocol, anything else used verbatim; www->apex 301 always; alias redirects add Cache-Control max-age=86400; mail.* containers excluded; containers ordered by descending domain-segment count; static containers keep backends but no ACLs; codepad ports 9000/9001 gated on SC_USE_CODEPAD + proxy_ports or -devel names; fixed ports 9200/8080/8443; cert dir literal /var/haproxy; cp -u only (no pruning except ca-bundle.pem). ARG '#cron.hourly' skips reload with exact msg. Exit codes: 4 (no SRVCTL), 44 (hs_only), 32 (argument), haproxy.js 0/111/99. Exact user-visible strings (msg/err listed in fact sheet, e.g. 'Regenerate haproxy configs.', 'wrote haproxy conf', 'reloaded haproxy.service', 'Container $ARG - $user ($reseller) - $OPA'). Datastore keys http-redirect/https-redirect/aliases/altnames/static/proxy_ports/http_port/https_port. rsyslog contract: local2 -> /var/log/haproxy.log, UDP 514, /etc/rsyslog.conf replaced. Global config values (maxconn 50000, 5m timeouts, ssl-server-verify none, gzip types, errorfiles from /var/www/html/<code>.http). regenerate_haproxy_conf must remain callable from the named module.

## letsencrypt
Must preserve exactly: systemd unit name acme-server.service plus its ExecStart/User=acme/Restart settings; ACME HTTP port 1028 and path prefix /.well-known/acme-challenge/ (HAProxy backend depends on both); acme user uid 528 and webroot /var/acme; /etc/letsencrypt/cli.ini field set (email=webmaster@SC_COMPANY_DOMAIN, text=True, authenticator=webroot, webroot-path=/var/acme); CA install path /etc/letsencrypt/ca.pem; bundle layout privkey then newline then fullchain then newline then ca then newline, written to SC_DATASTORE_DIR/cert/domain.pem; container deploy targets private/localhost.key, certs/localhost.crt (fullchain+ca), certs/domain.pem and certs/localhost.pem; exact certbot flags (certonly --non-interactive --agree-tos --keep-until-expiring --expand --webroot --webroot-path /var/acme/ -d domain optionally -d www.domain); main() skip rules (mail. prefix, subdomains path, .devel/-devel/.local/-local suffixes, dotless names); wildcard-cert path /etc/srvctl/cert/domain/domain.pem; 7-day checkend window 604800; get_le_dir lineage regex picking newest notAfter among domain and domain-NNNN; hook guard exit code 4; JS exit codes 99/0/100/111.

## mariadb
Lib file path is API: backupdb sources modules/mariadb/libs/mariadblib.sh by absolute path (backupdblib.sh:9); file must stay source-safe (top-level return at :194). Global side-effect contracts: SC_MDA (set at :32/:35), db_usr/db_pwd/db_name/dbd left for wordpress install-wordpress.sh:79,82, BACKUP_POINT (:57). Credential file formats byte-exact: /etc/mariadb-<db>.conf lines dbf:/usr:/pwd: re-parsed via grep 'pwd:' + ${db_pass:4} (:107-108); /etc/mysqldump.conf [client] user=root/password=... consumed by mysqldump --defaults-file and wordpress restore-wordpress-password.sh. Name derivation must be identical for existing production DBs: tr '.'->'_' and '-'->'_' (:100), user=first 15 chars, db=first 63 (:102-103), default name $HOSTNAME (:95). add_mariadb_db idempotency: early return when /etc/mariadb-$db_name.conf exists (:105-111). Backup layout /root/mariadb-dump/%Y_%m_%d__%H_%M_%S/<db>.sql with wipe-before-dump and information_schema/performance_schema exclusion (:55-77). check_mariadb_connection aborts the whole process (today with exit code 0 — fixing to nonzero is a behavior change). Activation: mariadb.service active OR inside nspawn/lxc container (module-condition.sh:3-9); SC_MARIADB_DUMP_CONF default /etc/mysqldump.conf, env-respected (:8-11).

## mozilla
Unit name/path /etc/systemd/system/mozilla-autoconfig.service (enabled fleet-wide; rename must handle the old enabled unit). Port 1029 and URL prefix /.well-known/autoconfig/mail/ are hardcoded contracts in haproxy.js:150,427. ExecStart bakes $SC_INSTALL_DIR/modules/mozilla/apps/mozilla-autoconfig-server.js into deployed units — moving the file crash-loops the service until the next update-install. Served XML must keep Content-Type text/xml, clientConfig version=1.1, literal %EMAILDOMAIN%/%EMAILADDRESS% placeholders (Thunderbird substitutes client-side), imap/pop3/smtp on 993/995/465, socketType SSL, password-cleartext. Hook filename hooks/update-install-host.sh and cached var SC_USE_MOZILLA (from dir name) are load-bearing; module-condition.sh must echo exactly 'true' via the haproxy->containers delegation (incl. update-install+ARG bootstrap). Hook exit-status propagates to exif; guard exits 4 when run directly. Transcript strings: 'Installing mozilla autoconfig' (install.sh:5) and run-echoed systemctl lines.

## named
Preserve: named.js exit protocol (0 ok, 111 with 'DATA-ERROR:' stderr prefix, 100 empty, initial 99) and exif message 'BIND/NAMED-ERROR' (libs/bashlib.sh:10); file paths /var/named/srvctl.conf, /var/named/srvctl/<domain>.zone (aliases share the primary's file on masters), <domain>.slave.zone/<alias>.slave.zone naming, peer cache /var/srvctl3/named/<host>.json, sources /var/srvctl3/datastore/containers.json and /etc/srvctl/clusters.json; fetch endpoint https://<host_ip>:443/.well-known/srvctl/datastore/containers.json with self-signed certs accepted (must match datastore-server + haproxy ACL); public zone semantics ($TTL 1D, epoch-seconds SOA serial, ns1/ns2.<SC_COMPANY_DOMAIN>, wildcard+@ A records, override_in_a_ip 'none' sentinel, SPF include order, gsuite MX set, default 'MX 10 mail' suppression rules, DKIM selectors default/mail/google/ml/k1-k3, 33-char DKIM splitting, DMARC p=reject and its suppression rules); datastore key names (override_in_a_ip, use_host_ip, spf_record, use_gsuite, use_mailchimp, use_mlsend, use_dmarc, dns_records, aliases, google-/facebook-verification, dkim-*-domainkey, host dns_server/host_ip/host_ipv6); skip rules (domain == SC_COMPANY_DOMAIN, dot-less names, first-seen-wins duplicates); activation only when SC_DNS_SERVER is master|slave; command name override-in-address with 'VE [none|ip]' syntax, exit 4 without $SRVCTL, exit 44 off-host, exit 32 missing arg; hook-facing function names namedcfg/restart_named/install_named; /etc/named.conf is overwritten on update-install and production masters currently depend on the /var/named/d250.conf include existing — changing it needs a migration plan.

## nfs
Mount-path contract /var/srvctl3/nfs/<host>/srv (nfslib.sh:26-27) is hard-coded in usersonhost/main.js:249 and must not change. /etc/exports exact content: '## $SRVCTL generated' header + '/srv 10.15.0.0/255.255.0.0(rw,no_root_squash)' with /srv as export root, since peers mount 10.15.<hostnet>.1:/srv. VPN address scheme 10.15.<hostnet>.1 (hostnet from datastore 'get host X hostnet'). firewalld service names nfs/mountd/rpc-bind; systemd units rpcbind + nfs-server enabled/restarted and symlinked under /etc/srvctl/system/. Hook names update-install-host/regenerate/diagnose and the fact that regenerate runs inside add-ve flows (must stay cheap: 1-second probe timeouts bound latency). All failures must remain non-fatal (err only); diagnose.sh:15's explicit 'return 0' is load-bearing because run_hook wraps sourced hooks in exif. Preserve logged strings: 'Could not mount $host 10.15.$hs.1', 'Could not ping $host on 10.15.$hs.1', 'Ping of $host failed', 'Install NFS', 'nfs mount', '-- NFS host shares --', 'mount check on  $host ($hs)' (double space). Activation must stay identical to the containers module condition, including the update-install+ARG bootstrap path.

## ntp
Keep: directory name `ntp` (drives SC_USE_NTP derivation, commonlib.sh:90) and hook filename hooks/update-install-host.sh (exact-match dispatch, commonlib.sh:100); module-condition.sh must echo exactly `true` to stdout (commonlib.sh:436), producing cached line `export SC_USE_NTP=true` in modules.conf; package name `ntpsec` and unit name `ntpd`, enable-then-start order; `run` wrapper's echoed prompt-style command line; failure contract where hook return status flows into exif → process exit with stderr "…/modules/ntp hook 'update-install-host' failed".

## odoo
Command name `install-odoo`; help block text verbatim incl. typo "it's basic dependencies" (install-odoo.sh:3-6); literal `root_only` token within first 20 lines (line 8) — commonlib.sh:219 greps for it to hide the command from non-root hints, and authlib exits 44. Module gating: condition stdout must be bare `true`/`false`; disabled for hostnames starting "mail.", else ve container detection (module-condition.sh:6-11). Filesystem/service contract: user odoo (system acct, home /srv/odoo); OCB clone from https://github.com/OCA/OCB.git branch 14.0 depth 1 at /srv/odoo/odoo14; dirs /srv/odoo/addons/{symlink,OCA}; venv /srv/venv; exact /srv/odoo/odoo14.conf contents (db_user=odoo, ports 8069/8072 on 127.0.0.1, proxy_mode True, workers 5, addons_path); unit name odoo.service with ExecStart /srv/venv/bin/python3 /srv/odoo/odoo14/odoo-bin -c /srv/odoo/odoo14.conf; enable set {postgresql, odoo, httpd}. Apache: re-run guard file /etc/httpd/conf.d/ssl.conf.org and ProxyPass mappings /longpolling/->8072, /->8069 retry=0 inside the SSL vhost. Postgres: pg_hba ident->md5, role `odoo with login createdb`, templates encoding 6 / hr_HR.UTF-8. Idempotency markers: /srv/odoo/.bashrc, pg_hba.conf, /srv/odoo/odoo14, /srv/venv, ssl.conf.org, /var/www/html/favicon.ico. Outputs: msg "Starting the odoo installer" (line 10), final "System succsesfully installed and services started!" with typo (line 373), non-root exit 1 (line 121). Keep disabled blocks disabled (wkhtmltopdf/MS fonts 165-175, OCA addon clones 229-249) and the LGPL header if body survives.

## opendkim
Preserve: datastore/runtime layout $SC_DATASTORE_DIR/opendkim/{TrustedHosts,KeyTable,SigningTable,<domain>/<sel>.private} mirrored to /var/opendkim (opendkimlib.sh:16) and the exact paths in /etc/opendkim.conf (update-install-host.sh:37-40); KeyTable/SigningTable/TrustedHosts line formats (opendkim.js:110-112, header 127.0.0.1/::1/10.0.0.0/8 at 73-75); selector rule mail.*→"mail" else "default" (opendkim.js:78-79); datastore values keep the literal "p=" prefix that named.js:211-236 depends on (opendkim.js:105-106); skip rule for .devel/-devel/.local/-local (opendkim.js:117); never regenerate an existing key — only when /srv/<domain>/opendkim/<sel>.private is absent (opendkim.js:86), /srv authoritative over datastore copy (101); containers.json serialized JSON.stringify(...,null,2) (130); perms: datastore dir 000 (opendkimlib.sh:8), /var/opendkim opendkim:opendkim, dirs 750, files 640 (17-22); Socket inet:8891@127.0.0.1 matching postfix hs-main.cf:82; restart only on diff, else leave running service alone (opendkimlib.sh:12-36); exif abort with "OPENDKIM-ERROR cfg" passing node's exit code (bashlib.sh:9); operator-visible strings ("regenerate opendkim", "Updating $HOSTNAME opendkim runtime configuration", "Configuration for opendkim is up-to-date...", "opendkim.service is not running!", "restarted opendkim.service", "opendkim restart FAILED!", "Wrote OpenDKIM TrustedHosts, KeyTable, SigningTable to ...").

## openvpn
Preserve exactly: generated paths /etc/openvpn/hostnet-server.conf, hostnet-client-$host.conf, hostnet-ccd/DEFAULT, hostnet-server.sh, dh2048.pem, and cert naming {NET}-ca.crt.pem / {NET}-server|client.{crt,key}.pem plus CA-side /etc/srvctl/CA layout; wire-format config directives (port 1101 udp, dev tun-hostnet / tun-host$hs, ifconfig 10.15.$SC_HOSTNET.1 and 10.15.$hs.$SC_HOSTNET, route 10.$hs.0.0/16 via 10.15.$hs.1, cipher AES-256-CBC, comp-lzo, topology subnet, client-config-dir hostnet-ccd) since mixed-version peers must handshake and nfs/ssh/datastore address hosts via 10.15.$hs.*; systemd instance names openvpn@hostnet-server, openvpn-server@hostnet-server, openvpn-client@hostnet-client-$host (persistently enabled on production); firewalld service name openvpn-hostnet; the cross-host RPC string `/bin/srvctl exec-function init_openvpn_create_ca_certificates $NET $HOSTNAME` (function name + arg order are a rolling-upgrade wire protocol); `sc openvpn <op>` fan-out behavior with return 0 after handling; update-install-host hook must end status 0 (run_hook exif aborts otherwise) including the errs-but-succeeds SC_ROOTCA_HOST-unset path; module activation on `-d /etc/openvpn` (which also activates the ca module).

## password
Function names get_password and new_password are cross-module API (mariadblib.sh:113,150,239; install-wordpress.sh:125; userlib.sh:45; add-user.sh:35; domaincertlib.sh:133) and must exist after load_libs. usersonhost/main.js:14 hard-requires the path modules/password/lib.js and export name get_password. new_password invokes /bin/node "$SC_INSTALL_DIR/modules/password/get-password.js" (bashlib.sh:8). Output contract: single stdout line, pattern Word-Word, charset [A-Za-z-] only — consumers embed it unescaped in SQL/configs. Failure contract: new_password exits the whole process via exif with node's exit code. module-condition.sh must echo exactly "true" (compared at commonlib.sh:437) yielding cached SC_USE_PASSWORD=true.

## perdition
Preserve: popmap line format "(.*)@<dom>: <mx>\n" and path /var/perdition/popmap.re (perdition.js:79,82); msg "datastore -> perdition popmap.re" and exit-code protocol 0/99/100/111 with "DATA-ERR R:" prefix (perdition.js:30-48); perditioncfg error string "PERDITION-ERROR cfg $* ($?) $__result" and its exif-abort semantics plus echoing node output (bashlib.sh:8-11); trailing "return 0" in hooks/regenerate.sh:7 (guards run_hook's exif against failed systemctl status in restart_perdition); unit names imap4/imap4s/pop3s in /etc/systemd/system with imap4 keeping --ssl_mode tls_outgoing --bind_address 127.0.0.1 and PID files under /var/run/perdition; exact /etc/perdition/perdition.conf directives incl. client-visible ok_line "Reverse-proxy IMAP4S service lookup OK!" and full-path map_library .so.0.0.0 (the .so.0 symlink does not work); restart messages "restarted X.service"/"X restart FAILED!" (incl. the existing "pop3 restart FAILED!" spelling, systemdlib.sh:37); host firewall services imaps+pop3s and mail-rootfs offline services imap+imaps+pop3s; activation identical to the containers module.

## postfix
Installed file contents must stay identical: conf/hs-main.cf -> /etc/postfix/main.cf plus appended lines "smtpd_tls_CAfile =    /etc/postfix/ca-bundle.pem" (four spaces, only if bundle exists; update-install-host.sh:18) and "smtpd_sasl_local_domain = $SC_COMPANY_DOMAIN" (:21); conf/hs-master.cf -> /etc/postfix/master.cf; conf/ve-main.cf -> container main.cf (relayhost=srvctl-gateway, home_mailbox=Maildir/, message_size_limit=26214400); conf/aliases -> /etc/aliases. Relaydomains: path /etc/postfix/relaydomains, '<domain>\tOK' lines from 'get cluster postfix_relaydomains' then postmap (postfixlib.sh:6-7; datastore/lib.js:949-951). Mail-pair convention: write_ve_postfix_conf writes main.cf for both X and mail.X keyed on the 5-char "mail." prefix (postfixlib.sh:15-24) and is called by containers addcontainerlib.sh:64 (public API name). Backup naming /srv/$C/rootfs/etc/postfix/main.cf-$NOW.bak with NOW=%Y.%m.%d-%H:%M:%S. firewalld service names exactly smtp and smtps, host always, image only when rootfs_name==mail. Package set spamassassin postfix amavisd-new; add_service postfix/amavisd/spamassassin (enable+restart+status+/etc/srvctl/system symlink). systemctl restart (not reload) of postfix.service on regenerate; hook failure exits the whole command via exif with systemctl's exit code. Silent return 0 when /srv/$container or rootfs/etc/postfix is missing (postfixlib.sh:32-40). User-visible strings incl. the typo "err \"$container dont exists\"" (postfixlib.sh:62), "Installing postfix.", "Writing postfix configuration for $container", "Postfix restart FAILED!", "restarted postfix.service".

## saslauthd
Guard exit 4 in both commands ([[ $SRVCTL ]] || exit 4); exit 44 from root_only/hs_only; exact /etc/sasl2/smtpd.conf content (pwcheck_method: saslauthd, mech_list: LOGIN — postfix hs-main.cf smtpd_sasl_type=cyrus depends on it); exact /etc/sysconfig/saslauthd values (SOCKETDIR=/run/saslauthd, MECH=rimap, FLAGS="-n 0 -O localhost -r" — the -r realm flag is load-bearing for user@domain auth via perdition); unit semantics (Type=forking, PIDFile=/run/saslauthd/saslauthd.pid, EnvironmentFile, ExecStart with $SOCKETDIR/$MECH/$FLAGS, RuntimeDirectory=saslauthd); command names fix-saslauthd and testsaslauthd plus their @en hints; user-visible strings ("Successfully restarted saslauthd", "Saslauthd Restart FAILED", "restarted saslauthd.service", "saslauthd restart FAILED!", "Mission failed.", "Container $domain"/"Container mail.$domain", "Installing saslauthd - binary for x86_64"); password lookup precedence /srv/$domain then /srv/mail.$domain overrides; lib function name restart_saslauthd (called by regenerate hook); add_service side effects (symlink /etc/srvctl/system/saslauthd.service, enable+restart+status).

## srvctl
Exit codes: customize exit 23 (no ARG); argument exit 32; root_only/reseller_only exit 44; update-install exit 5 after first-hostname set (install automation reboots on it) and exit 0 on the no-ARG host path; service ops exit 0 via exit_0. Strings: "ASSUME system-service:/user-service: $service"; "$HOSTNAME update-install complete"; "update-install complete. please reboot."; "-- software-versions --" and msg_version_installed's "$1 ${v:13:8} ${i:13}" format; "srvctl version $(cat version)"; root_only's stdout "SC_UID0 true". Markup: ## @@@ / ## @en / ## &en offsets (:7) and the "## spec //services×…" lines in command.sh consumed by gui spec.sh; literal root_only/reseller_only tokens in first 20 lines gate help visibility. Paths/formats: /etc/bash_completion.d/srvctl-completion; /var/local/srvctl/completion/$USER.{hints,commands,arguments,units,VE} word-list files (read by completion.sh and cat'ed at every shell login); /etc/srvctl/system/$NAME.service symlinks; /etc/srvctl/debug.conf DEBUG=true; $SC_HOME/srvctl-includes/$arg.sh + .srvctl/srvctl-includes.bak/$arg-$NOW.sh. Semantics: start/restart both = systemctl enable+restart; stop does NOT disable (only disable does); bare `sc SERVICE` = journalctl --since yesterday; wheel-group users allowed; ? ! + - aliases mapped in srvctl.sh core; update-install runs dnf update even without ARG, and init.sh regenerates /etc/srvctl from /etc/srvctl/data and re-tests modules BEFORE the command body; hook order pre-update-install-host -> update-install-host -> post; sudomize re-exec `sudo $SC_INSTALL_DIR/srvctl.sh <args>` matches usersonhost sudoers entry "$SC_INSTALL_DIR/srvctl.sh *"; sc complicate prints "srvctl command-completion has been updated for $SC_USER@$HOSTNAME".

## ssh
Preserve exactly: AuthorizedKeysCommand line with hardcoded /usr/local/share/srvctl/modules/ssh/sshd_authorization.sh %u and AuthorizedKeysCommandUser root (sshd_config:51-52); sshd_authorization.sh's three hardcoded source paths and unconditional exit 0 (share/common/authorized_keys also read by sshpiperd workingdir.go:176); drop-in filenames /etc/ssh/ssh_config.d/srvctl-chosts.conf and srvctl-containers.conf with StrictHostKeyChecking no + UserKnownHostsFile /var/srvctl3/ssh/known_hosts contents; both known_hosts paths and line formats incl. 10.15.<hostnet>.<hostnet> and short-hostname aliases; copied key naming share/containers/<C>/users/<U>/<U>-<origfile> incl. .hash files (codepad reads the same tree); host_key fields in hosts.json/containers.json and /srv/<C>/host_key cache; ssh.js exit 111 with DATA-ERROR: prefix and exif abort in ssh_main; regenerate must keep purging share/containers/*/users/*/authorized_keys; update_install_ssh_config's SC_HOSTNET guard, sshd enable/restart, and hook exit status coming from `run systemctl status sshd`; PermitRootLogin yes + PasswordAuthentication no in deployed sshd_config.

## sshpiperd
Preserve exactly: /bin/sshpiperd binary path; unit /etc/systemd/system/sshpiperd.service with ExecStart=/bin/sshpiperd --server_key /etc/sshpiper/ssh_host_rsa_key, User/Group=sshpiper, Restart=always, RestartSec=3; host-key path /etc/sshpiper/ssh_host_rsa_key (ECDSA despite the rsa name — regenerating/renaming breaks host-key pinning for all port-2222 users); system user 'sshpiper' and sshpiper:root ownership of /etc/sshpiper; bindfs invocation 'bindfs -r -p +X --map=root/sshpiper /var/srvctl3/datastore/users /var/sshpiper' and mountpoint /var/sshpiper (daemon default WorkingDir); firewalld service name 'sshpiperd' tcp/2222; composite username format user_container_account with regexp ^[a-z][-a-z0-9\.]{0,31}_[-a-z0-9\.]{0,256}_[-a-z0-9]{0,31}$ and fixed upstream <container>:22; datastore key contracts <user>/*.pub, <user>/srvctl_id_ecdsa and /var/srvctl3/share/common/authorized_keys (shared with ssh/usersonhost/gui modules); 0077 perm check on the mapped private key; module activation identical to the containers module.

## static
Preserve: systemd unit name/path /etc/systemd/system/static-server.service with ExecStart=/bin/node $SC_INSTALL_DIR/modules/static/server.js, User/Group=root, Restart=always (update-install-host.sh:11-25) and removal of legacy /usr/lib/systemd/system unit (line 9); listen port 1280 plain HTTP (server.js:42); docroot layout /var/srvctl3/storage/static/<host>/html (server.js:37, libs/regenerate.sh:7 — containers remove-ve.sh:63/destroy-ve.sh:47 rm -fr against it); Host normalization (undefined→d250.hu, strip www./static., server.js:17-22); /var/srvctl3/storage created or gluster-mounted (volume srvctl-storage) at init with unconditional return 0 (hooks/init.sh); function name regenerate_static_server, its create-only-if-index.html-missing rule and title arg "$dir (static)" (libs/regenerate.sh:3-10); iteration over `get cluster container_list` (cluster-wide); output strings "Regenerate static fileserver structure", "regenerate static server index files", "srvctl static-server started", and the per-request log format; global npm finalhandler/serve-static required from /usr/lib/node_modules; activation identical to containers module (module-condition.sh:3 sources it).

## usersonhost
Preserve datastore layout/filenames under $SC_DATASTORE_DIR/users/<user>/ (.password, .hash = unsalted sha512 hex no newline, id_ecdsa[.pub], srvctl_id_ecdsa[.pub], reseller_id_ecdsa.pub, srvctl_reseller_id_ecdsa.pub) and exact SSH key comment strings `<user>@<SC_COMPANY_DOMAIN> (id_ecdsa <HOSTNAME> <NOW>)` / `(srvctl <HOSTNAME> <NOW>)`. Keep add-publickey path `$SC_DATASTORE_DIR/users/$SC_USER/$SC_USER-$NOW.pub` and NOW format %Y.%m.%d-%H:%M:%S. Keep client cert `/home/<user>/<user>@<SC_COMPANY_DOMAIN>.p12` from /etc/srvctl/CA/usernet/client-<user>.p12. Keep share dirs ~/<container>/bindfs (root uid) and ~/<container>/html (root uid+48, skipped for mail.* containers) and bindfs flags `--mirror=<user> --create-for-user/group=<uid>` plus the `<src> on <dir> type fuse` mount-detect string. Keep exit codes 22 (invalid username / missing username), 23 (unknown user in change-user). Keep help text incl. typos 'argiument'/'publikkey', and change-user's `DEV (not implemented - yet.)`. Keep /etc/sudoers.d/srvctl exact contents (`## srvctl v3 sudo file` + `ALL ALL=(ALL) NOPASSWD: $SC_INSTALL_DIR/srvctl.sh *`) and the user-tools package list with /usr/bin guards.

## usersonve
Preserve: command names + exact ## @@@ / @en / &en help lines for all 5 commands; exit codes (exit 4 SRVCTL guard in 4 commands — not the conventional 10; exit 22 invalid username add-user.sh:16; exit 32 argument; exit 44 ve_only/root_only; exit 1 devicemap lookup failure add-zerotier.sh:28); operator-visible strings 'Password for $username is: $password' (add-user.sh:38), mail subject 'Welcome to $HOSTNAME' (add-user.sh:43); add-user's add-or-update contract where an existing $home/.password is reused verbatim on re-run; generated file paths/contents: /home/x/autostart.sh as the kiosk entry point referenced by /usr/share/xsessions/none.desktop and the Ctrl+Alt+Del xbindkeys binding, /home/x/.vnc/config keys (desktop=\"D250 Laboratories\", securitytypes=none, geometry=1920x1080, session=$DESKTOP), /etc/tigervnc/vncserver.users ':0=x', systemd unit name 'vncserver' with ExecStart=/usr/libexec/vncsession-start :0 + SELinuxContext + Restart=always, /etc/yum.repos.d/mcallegari79.repo (repo id home_mcallegari79, Fedora_38), /bin/crossover and license symlinks, /var/lib/zerotier-one/devicemap symlink; shared host asset paths /var/srvctl3/share/common/{crossover,qlcplus,zerotier-one}; permanent firewalld state (vnc-server service, ZeroTier trusted-zone interface); module visible only inside containers (ve condition); add-zerotier.sh is USER WIP and excluded from any rewrite.

## ve
(1) modules/ve/module-condition.sh is a cross-module API: sourced by modules/usersonve/module-condition.sh:3 inside $(source ...); must stay at that path, remain source-able (top-level `return`), and print exactly "true" or "false" as its only stdout (compared == true at commonlib.sh:437). (2) Directory name "ve" derives the cached flag SC_USE_VE written to modules.conf on deployed servers (commonlib.sh:431,448). (3) lxc must remain accepted as a container (module-condition.sh:6). (4) Command name `status` plus the `sc ?` alias (srvctl.sh:92) must resolve to this file on VEs, while containers/commands/status.sh keeps winning first-match dispatch on hosts (alphabetical SC_MODULES order, srvctl.sh:72-75). (5) Exact status output sequence and strings: msg "$HOSTNAME running.", msg "connected users:", raw `w`, msg "Disk usage:", three raw `du -hs` lines for /home /srv /var in that order; hint string "List container status parameters". (6) Env defaults SC_ROOTFS_DIR=/var/srvctl3/rootfs and SC_MOUNTS_DIR=/var/srvctl3/mounts only when unset — /etc/srvctl/*.conf overrides must keep winning; SC_VIRT becomes readonly in the main shell on VEs, so a rewrite must not assign SC_VIRT later in the same shell.

## vncproxy
Frozen algorithm: hash() in vncproxy.js:67-86 (exact 77-char chars string, Math.imul(...,9**9) with seed 9, abs(h^(h>>>9)).padStart(10,"0"), overlapping substrings 0-3/2-5/4-7/6-9 mod 77, 8-char truncation) — any change invalidates every deployed VNC password. Preserve: /var/vncproxy/records path and exact line format 'record "KEY" "C:5900" "null" "USER @ C"' (executed as bash and hand-edited on servers); /var/vncproxy/vncproxy.db and schema vncproxy(forward_key VARCHAR(8) PRIMARY KEY, dest_addr TEXT NOT NULL, dest_passwd VARCHAR(8), comment TEXT) read by the C++ binary; /bin/vncproxy path and CLI 'vncproxy <host:port> [db]'; bind $SC_HOST_IP:5900 and dest '<container>:5900'; unit name vncproxy.service with ExecStart=bash /usr/local/share/srvctl/modules/vncproxy/start.sh, Restart=always, RestartSec=3; hand-installed restarter units referencing /usr/local/share/srvctl/modules/vncproxy/vncproxy-restarter.sh; command name add-vnc-user with its @@@/@en/&en help block, exit 22 + 'Invalid username: $username', hs_only gating (exit 44), final exit = systemctl status result; outputs 'User added. container: C vncuser: U host: H password: XXXXXXXX' (only place the password is shown), 'wrote vncproxy conf', 'OK: vncproxy IP:5900', 'OK:/FAIL: nmap -p 5900 ...', 'VNCPROXY-ERROR cfg ...', 'DATA-ERROR:' exit 111; datastore key containers[C].vncusers and verb 'add container C vncuser NAME'; global bash function name vncproxycfg.

## wordpress
Help metadata verbatim (install-wordpress.sh:3-6 incl. the "it's" typo); command name `install-wordpress` (invoked literally by recreate-ve.sh:61 over ssh); activation contract SC_USE_WORDPRESS = every container except hostname prefix "mail."; root_only exit code 44 and its "SC_UID0 true" stdout; the final line `Wordpress @ https://$HOSTNAME/wp-admin admin:$password` (operators copy the password from it); generated paths and contents: /var/www/html/wp-config.php and wp-content (rsync'd by recreate-ve.sh:68-69), /var/www/html/.admin mode 000 (read by restore script), wp-install.php, /etc/httpd/conf.d/wp-permalink.conf and logging-behind-reverse-proxy.conf with "## srvctl generated" headers, deletion of /etc/httpd/conf.d/wordpress.conf and /var/www/html/index.html; DB name `<first-hostname-label>_wp` sanitized ./-→_ plus /etc/mariadb-<db>.conf side effect; table_prefix wp_, admin user "admin", email root@localhost, blog title $HOSTNAME, FORCE_SSL_ADMIN true, DB_HOST localhost; chown -R apache:apache /var/www/html; mariadb+httpd enabled/restarted with /etc/srvctl/system symlinks; redefined `combined` LogFormat consumed by log tooling; restore script's documented invocation path under /usr/local/share/srvctl/modules/wordpress/scripts/.
